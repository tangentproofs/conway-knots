# Campaign journal

Narrative log of the Conway rational-tangle formalization campaign: what was
tried, what landed, what was refuted, and the decisions taken. Newest entries
at the bottom. Each entry is append-only — never rewrite history; correct the
record with a new entry.

## 2026-09-04 — WSL2 rescue

The Autoform MCP servers (`autoform-lsp`, `autoform-repl`) cannot start on
native Windows: `servers/lean_client.py` requires `socket.AF_UNIX` and
`os.getuid`, both absent on Win32, so both servers exit 1 with
`LeanRuntimeError`. Installed WSL2 + Ubuntu (user `michal`) after a wedged-VM
detour that needed a reboot. Set up native ext4 checkouts
(`~/wrk/autoform-bot`, `~/ver/conway-knots`), `uv`, `elan` (Lean 4.32.2),
and native Linux `opencode`. Both MCPs connect from WSL. Windows-side
`opencode.json` remains natively broken by design. Recorded in `AGENTS.md`.

## 2026-09-04 — Blueprint site live

`autoform render` + `mkdocs build` + serve. Site browsable locally; later
replaced `mkdocs serve` with a static server (the dev server 404'd deep
pages) and published to `gh-pages` via `mkdocs gh-deploy`:
https://tangentproofs.github.io/conway-knots/

## 2026-09-04 — Agent review, then full steam

Chose `agent-review` over grinding lemmas. Baselines: `lake build` green
(1001 jobs), zero sorries, `autoform check` 52 articles (24 fully proved),
`autoform audit` with kind-mismatch/overfull/cycle findings. Then: fixed
declaration kinds, split the chapter to break a dependency cycle
(colouring chapter vs theorems chapter, via moving the flipping lemma),
reorganized into foundations/forms/toolkit/classification chapters.

## 2026-09-05 — Theorem 4 capstone + splits

New `ColoringFractionTheorem.lean`: 8 thin paper-numbered wrappers around
proved Theorem-4 fragments. Wired Theorems 4/3/2 nodes with honest
boundaries (no `proof:` flags on assemblies). Split 6 oversized nodes into
~40 PR-sized sub-nodes + 7 sub-chapters. Audit clean.

## 2026-09-05 — Wiring campaign

Round after round of proved-but-unclaimed lemmas wired into the roadmap:
`[0]`/`[∞]` summands, canceling units, nested chains, two-block sums,
glue toolkit, port toolkit, standard-values chapter. Discovered and
documented an upstream audit-scanner bug (`mutual`/`end` blindness);
relocated one theorem pre-`mutual` to reclaim it rather than touching
tooling. Coverage declared complete at ~130 articles.

## 2026-09-05 — Sharpness: the wall is real

Proved the hoped-for transports are FALSE in general, not merely unproved:
mirror transport fails already on one crossing
(`not_coloring_mirror_transport`); invert transport holds at one crossing
but fails at two (`not_coloring_invert_transport`); `DiagonalSum` fails on
degenerate gluings. These turned "outstanding" into "refuted" — equally
valuable, and they constrain all future strategy.

## 2026-09-06 — Fragment chain 4 → 2 → 1 closed

Scope decision (owner): close Theorems 4 → 2 → 1 at fragment scope; park
Theorem 3 general. Three review rounds verified every prose claim against
its Lean statement, fixed drift (including a local-vs-global RIII
overstatement), and set `proof: formalized` on all three. Theorem 1 reached
`fully_proved` by re-pointing its proof-deps to flagged fragment nodes.
126 fully proved, audit clean. Committed and pushed (`76792ca`), site
redeployed.

## 2026-09-08 — Theorem 3 un-parked; wall upgraded to theorem

Attempted the general case via CF-canonicalization and alternate isotopy
paths. Both routes analysed and ruled out; instead proved
`TwistExpr.mulTop_two_inner_ne_toStandard` (inner F = 2 gives algebraic
1/3 vs standard-form 2/3): the agreement hypotheses are NECESSARY. With the
earlier ±1 necessity result, blocker (b) is closed on the finiteness
fragment — by theorem, not by gap. General Theorem 3 stays unproved, now
provably unclosable via agreement-dropping.

## 2026-09-08 — Journal started

Backfilled from session history. Standing rule: the worker appends a dated
entry every round (append-only).

## 2026-09-08 — Negative three-unit invert-add value

Proved ([-1]x3) inv carries -1/3: universal (rules force d/-3d) plus direct witness, mirroring a20 with fresh mirror-switch PD derivations. New wrinkle vs positive case: the denominator multiple is negative, so the nonzero transfers need neg_ne_zero (mul_ne_zero does not fire under a leading negation) — two-line fix, then green. Module build 22.9s vs ~19s positive baseline.
Key files/theorems: DiagonalSumGeneral.lean tail (coloring_fraction_invert_add_negOne_negOne_negOne, sharpColAddNegThree, HasColoringFraction.invert_add_negOne_negOne_negOne); new node invert-units/a24-neg-three-unit-value.md. Check 150 articles/127 fully proved, audit clean, render 174 pages/131 nodes. Decision: negative ladder rung one landed; rung two ([-1]x4 at -1/4) or the unitChain inductive universal are both open next.

## 2026-09-09 — Negative seven-unit rung verified and wired

Prior round's ([-1]x7) proofs were written but never built (shell prompts blocked verification). Fixed two `set_option maxRecDepth 1024 in` placements (must precede the docstring, not follow it), after which the full build went green (1005 jobs, DiagonalSumGeneral in 16s). Wired a29a (universal) + a29b (existence) into neg-ladder/. Check 160 articles/136 fully proved, audit clean. Permission config widened (D:/ver/**, wsl *) and campaign scripts consolidated into scripts/ to stop the per-command prompts.

## 2026-09-09 — Positive ladder closed by unitChain induction

The collapse lemma went through on the first serious attempt: NotMono transfers from rung n+1 to the prefix rung (if the prefix were monochrome, the appended switched rule forces the whole rung monochrome — pure linarith after the last-crossing equations), and coloring restriction is prefix membership. Landed the full package in the DiagonalSumGeneral.lean tail: matrix/restrict/collapse/rung-zero preliminaries, the universal `coloring_fraction_unitChain_invert` (every non-monochrome coloring of `(unitChain n).invert` has fraction 1/(n+2)), the integer packaging `unitChain_invert_ladder_eq` (denominator (n+2) x numerator, both nonzero), and existence `HasColoringFraction.unitChain_invert` (forced if-else extension at the two fresh arcs; prefix arcs bounded by arc_le_maxArc_of_mem + maxArc_invert + unitChain_maxArc, no custom bound lemma needed). Two build iterations: one precedence bug (`/` escaping `ofRat` in hfracNext, HDiv error at then-line 2340), then green 1005 jobs, zero sorry/admit/axiom, no new warnings. Audit first flagged node-too-large (7 theorems spanning 363 lines vs 79.5 median), so the single a25 node was split into a25a-collapse / a25b-universal / a25c-existence (each under 120 lines). Final: check 153 articles/130 fully proved, audit clean True, render 177 pages/134 nodes. No fallback to negative rung two needed. Decision: negative ladder ([-1]x4 at -1/4, neg_ne_zero pattern) is the natural next rung; the negative-family induction is now the open question (mirror-switch wirings differ per rung, so no shared unitChain analogue yet).

## 2026-09-09 — Negative ladder rung two: ([-1]x4) inv carries -1/4

Proved ([-1]x4) inv carries -1/4: universal (rules force d/-4d) plus direct witness, mirroring a21 with fresh mirror-switch PD derivations re-derived from the add/invert definitions (never negated positive codes): non-inverted crossings [⟨1,2,3,0,neg⟩, ⟨5,6,2,1,neg⟩, ⟨8,9,6,5,neg⟩, ⟨11,12,9,8,neg⟩] switch to [⟨2,3,0,1,pos⟩, ⟨6,2,1,5,pos⟩, ⟨9,6,5,8,pos⟩, ⟨12,9,8,11,pos⟩], matrix ⟨col11,col12,col0,col3⟩, fraction (col12-col11)/(col12-col3). Same neg_ne_zero pattern as rung one for the negative denominator multiple. Two build iterations: first failed on a dropped fourth `.add` line in the witness `hM` (three-unit diagram, unbalanced parens — caught by Lean, fixed, re-read); then green 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). Witness extends the rung-one coloring by the forced pair (col11,col12)=(-2,-1), matrix ⟨-2,-1,2,3⟩. New node invert-units/a26-neg-four-unit-value.md (block span 154 lines, under 200, namespace-aware). Cost: full WSL `lake build` 30.2s wall, DiagonalSumGeneral module 15s vs 10s at the a25 baseline (rung-one figures were 22.9s module vs ~19s positive) — the direct-computation method keeps scaling roughly linearly per rung. Check 154 articles/131 fully proved, audit clean, render 178 pages/135 nodes. Decision: negative ladder rung three ([-1]x5 at -1/5) or the negative-family induction question are both open next.

## 2026-09-09 — Negative ladder rung three: ([-1]x5) inv carries -1/5

Proved ([-1]x5) inv carries -1/5: universal (rules force d/-5d) plus direct witness, mirroring a22 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): fifth base crossing ⟨14,15,12,11,neg⟩ follows the add-gluing pattern (a2/a3 rejoin the prior rung's fresh arcs 12/11, fresh arcs 14/15), switching to ⟨15,12,11,14,pos⟩; matrix ⟨col14,col15,col0,col3⟩, fraction (col15-col14)/(col15-col3). Same neg_ne_zero pattern as rungs one/two for the negative denominator multiple. Single build iteration — green first try, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). Witness extends the rung-two coloring by the forced pair (col14,col15)=(-3,-2), matrix ⟨-3,-2,2,3⟩. New node invert-units/a27-neg-five-unit-value.md (block span 180 lines, under 200, namespace-aware — single node, no split needed). Cost: full WSL `lake build` 30.6s wall, DiagonalSumGeneral module 16s vs 15s at rung two (10s at the a25 baseline) — the direct-computation method keeps scaling roughly linearly per rung. Check 155 articles/132 fully proved, audit clean True (findings []), render 179 pages/136 nodes. Decision: negative ladder rung four ([-1]x6 at -1/6) or the negative-family induction question are both open next.

## 2026-09-09 — Negative ladder rung four: ([-1]x6) inv carries -1/6

Proved ([-1]x6) inv carries -1/6: universal (rules force d/-6d) plus direct witness, mirroring a22/a27 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): sixth base crossing ⟨17,18,15,14,neg⟩ rejoins the prior rung's fresh arcs 15/14 per the add-gluing pattern (fresh arcs 17/18), switching to ⟨18,15,14,17,pos⟩; matrix ⟨col17,col18,col0,col3⟩, fraction (col18-col17)/(col18-col3). Same neg_ne_zero pattern as rungs one–three for the negative denominator multiple. Single build iteration — green first try, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). Witness extends the rung-five coloring by the forced pair (col17,col18)=(-4,-3), matrix ⟨-4,-3,2,3⟩. Cost: WSL `lake build RationalTangles.DiagonalSumGeneral` 27.6s wall (module itself 18s, 1000 jobs), full `lake build` +8.7s incremental, 1005 jobs green.
Span check forced a split: the combined block is 207 lines (over the 200 limit), so universal (128 lines) and existence (78 lines) wired as a28a/a28b. Audit then flagged overfull-container on the invert-units parent (25 articles vs the 24 limit); fixed honestly per the sub-chapter rule by grouping rungs three–six into the new `neg-ladder/` sub-chapter (a24, a26, a27, a28a, a28b + README; moved nodes' Depends links deepened one level), parent now lists one link (21 children). Check 158 articles/134 fully proved, audit passed (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 183 pages/138 nodes/534 code links. Decision: negative ladder rung five ([-1]x7 at -1/7) follows the same pattern; the negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung six: ([-1]x8) inv carries -1/8

Proved ([-1]x8) inv carries -1/8: universal (rules force d/-8d) plus direct witness, mirroring a29 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): eighth base crossing ⟨23,24,21,20,neg⟩ rejoins the prior rung's fresh arcs 21/20 per the add-gluing pattern (fresh arcs 23/24), switching to ⟨24,21,20,23,pos⟩; matrix ⟨col23,col24,col0,col3⟩, fraction (col24-col23)/(col24-col3). Same neg_ne_zero pattern as rungs one–five for the negative denominator multiple; `set_option maxRecDepth 1024` prefix on both theorems (required since rung five, placed before the docstrings). Single build iteration — green first try, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). Witness extends the rung-five coloring by the forced pair (col23,col24)=(-6,-5), matrix ⟨-6,-5,2,3⟩. Span check done upfront: universal 173 lines, existence 86 lines, so wired as a30a/a30b in neg-ladder/ from the start (combined block ~271 lines, over the 200 limit). Cost: repo-script WSL `lake build` green, DiagonalSumGeneral module 20s vs 16s at rung five (10s at the a25 baseline) — the direct-computation method keeps scaling roughly linearly per rung. Check 162 articles/138 fully proved, audit clean (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 187 pages/142 nodes/538 code links. Record correction: the prior rung-five wiring (a29a/b, [-1]x7) never got a JOURNAL entry; the pre-change check this round (160 articles/136 fully proved) corroborates its landed state. Decision: negative ladder rung seven ([-1]x9 at -1/9) follows the same pattern; the negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung seven: ([-1]x9) inv carries -1/9

Proved ([-1]x9) inv carries -1/9: universal (rules force d/-9d) plus direct witness, mirroring a30 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): ninth base crossing ⟨26,27,24,23,neg⟩ rejoins the prior rung's fresh arcs 24/23 per the add-gluing pattern (fresh arcs 26/27), switching to ⟨27,24,23,26,pos⟩; matrix ⟨col26,col27,col0,col3⟩, fraction (col27-col26)/(col27-col3). Same neg_ne_zero pattern as rungs one–six for the negative denominator multiple; `set_option maxRecDepth 1024` prefix on both theorems (required since rung five, placed before the docstrings) sufficed at depth 9 — no bump needed. Single build iteration — green first try, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). Witness extends the rung-eight coloring by the forced pair (col26,col27)=(-7,-6), matrix ⟨-7,-6,2,3⟩. Span check done upfront: universal 197 lines, existence 94 lines, so wired as a31a/a31b in neg-ladder/ from the start (combined block ~300 lines, over the 200 limit; universal used a 2-line docstring to hold 197). Cost: repo-script WSL `lake build` green, DiagonalSumGeneral module 17s vs 20s at rung eight (10s at the a25 baseline) — the direct-computation method keeps scaling roughly linearly per rung. Check 164 articles/140 fully proved, audit clean (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 189 pages/144 nodes/540 code links. Decision: negative ladder rung eight ([-1]x10 at -1/10) follows the same pattern — universal span is now at 197/200, so rung ten will need a further size strategy (tighter docstring is exhausted; consider a shared preliminary or per-crossing membership lemma); the negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung eight: ([-1]x10) inv carries -1/10 via factored preliminaries

Proved ([-1]x10) inv carries -1/10: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a31 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): tenth base crossing ⟨29,30,27,26,neg⟩ rejoins the prior rung's fresh arcs 27/26 per the add-gluing pattern (fresh arcs 29/30), switching to ⟨30,27,26,29,pos⟩; matrix ⟨col29,col30,col0,col3⟩, fraction (col30-col29)/(col30-col3). Same neg_ne_zero pattern as rungs one–seven for the negative denominator multiple; `set_option maxRecDepth 1024` prefix on all three declarations (placed before the docstrings) sufficed at depth 10 — no bump needed.
Size strategy (as flagged last round): the flat universal projected to ~210 lines, so the ten rule equations were factored into `invert_add_negOne_..._rules` (52 lines: one crossings-list `decide`, `simp only [TangleDiagram.IsColored, hcs] at hc`, then each rule read off with its explicit crossing so no arena carries the 10-unit expression more than twice); the universal (~98 lines) obtains over it and runs the usual linarith tail; the existence (~101 lines: `sharpColAddNegTen` + standard witness) stays self-contained. All three blocks under 200, namespace-aware. Single build iteration — green first try, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). Witness extends the rung-nine coloring by the forced pair (col29,col30)=(-8,-7), matrix ⟨-8,-7,2,3⟩. Cost: repo-script WSL `lake build` green, DiagonalSumGeneral module 17s (same as rung nine; 10s at the a25 baseline) — the factored method holds module time flat this round. Check 167 articles/143 fully proved, audit clean (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 192 pages/147 nodes/543 code links. Decision: negative ladder rung nine ([-1]x11 at -1/11) follows the same factored pattern (extend the prelim list by one +3-arc crossing); the negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung nine: ([-1]x11) inv carries -1/11 via factored preliminaries

Proved ([-1]x11) inv carries -1/11: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a32 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): eleventh base crossing ⟨32,33,30,29,neg⟩ rejoins the prior rung's fresh arcs 30/29 per the add-gluing pattern (fresh arcs 32/33), switching to ⟨33,30,29,32,pos⟩; matrix ⟨col32,col33,col0,col3⟩, fraction (col33-col32)/(col33-col3). Same neg_ne_zero pattern as rungs one–eight for the negative denominator multiple.
Depth wrinkle (new this round): `set_option maxRecDepth 1024` no longer suffices at depth 11 — the universal's `hm`-typing step and the existence `rfl` matrix computation both hit maximum-recursion-depth errors, while the preliminary's `decide` still passes at 1024. Bumped exactly the two failing declarations to `set_option maxRecDepth 2048` (placed before the docstrings); prelim stays at 1024. Two build iterations: first failed only on the two depth errors (caught by Lean, fixed, re-read); then green, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). Witness extends the rung-ten coloring by the forced pair (col32,col33)=(-9,-8), matrix ⟨-9,-8,2,3⟩. Spans checked upfront: prelim ~59 lines, universal ~105 lines, witness+existence ~121 lines, all under 200 namespace-aware — wired as a33/a33a/a33b in neg-ladder/ from the start. Cost: repo-script WSL `lake build` green, DiagonalSumGeneral module 20s (same as rung eight; 10s at the a25 baseline), full build 39s wall on the final iteration. Check 170 articles/146 fully proved, audit clean (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 195 pages/150 nodes/546 code links. Decision: negative ladder rung ten ([-1]x12 at -1/12) follows the same factored pattern (extend the prelim list by one +3-arc crossing); expect maxRecDepth pressure to persist at 2048. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung ten: ([-1]x12) inv carries -1/12 via factored preliminaries

Proved ([-1]x12) inv carries -1/12: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a33 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twelfth base crossing ⟨35,36,33,32,neg⟩ rejoins the prior rung's fresh arcs 33/32 per the add-gluing pattern (fresh arcs 35/36), switching to ⟨36,33,32,35,pos⟩; matrix ⟨col35,col36,col0,col3⟩, fraction (col36-col35)/(col36-col3). Same neg_ne_zero pattern as rungs one–nine for the negative denominator multiple.
Depth setup (preemptive per standing orders): all three declarations start at `set_option maxRecDepth 2048` (placed before the docstrings) — the rung-eleven round showed 1024 failing on the universal `hm`-typing and existence `rfl` steps, and the twelve-crossing preliminary `decide` is strictly larger, so the prelim was bumped too rather than re-proving the failure. Single build iteration — green first try, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). Witness extends the rung-eleven coloring by the forced pair (col35,col36)=(-10,-9), matrix ⟨-10,-9,2,3⟩. Spans: prelim 62 lines, universal 123 lines, witness+existence ~132 lines, all under 200 namespace-aware — wired as a34/a34a/a34b in neg-ladder/ from the start. Cost: repo-script WSL `lake build` green, DiagonalSumGeneral module 23s (20s at rung nine; 10s at the a25 baseline), full build ~2min wall with replayed-module warnings only. Check 173 articles/149 fully proved, audit clean (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 198 pages/153 nodes/549 code links. Decision: negative ladder rung eleven ([-1]x13 at -1/13) follows the same factored pattern (extend the prelim list by one +3-arc crossing); expect maxRecDepth 2048 pressure to persist. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung eleven: ([-1]x13) inv carries -1/13 via factored preliminaries

Proved ([-1]x13) inv carries -1/13: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a34 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirteenth base crossing ⟨38,39,36,35,neg⟩ rejoins the prior rung's fresh arcs 36/35 per the add-gluing pattern (fresh arcs 38/39), switching to ⟨39,36,35,38,pos⟩; matrix ⟨col38,col39,col0,col3⟩, fraction (col39-col38)/(col39-col3). Same neg_ne_zero pattern as rungs one–ten for the negative denominator multiple.
Depth setup (preemptive per standing orders): all three declarations start at `set_option maxRecDepth 2048` (placed before the docstrings) — required since rung ten, no new pressure observed at depth 13. Single build iteration — green first try, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). Witness extends the rung-twelve coloring by the forced pair (col38,col39)=(-11,-10), matrix ⟨-11,-10,2,3⟩. Spans checked upfront: prelim 66 lines (4700–4765), universal ~114 lines (4767–~4880), witness+existence ~138 lines (4882–5019), all under 200 namespace-aware — wired as a35/a35a/a35b in neg-ladder/ from the start. Cost: repo-script WSL `lake build` green first try, DiagonalSumGeneral module 24s (23s at rung ten; 10s at the a25 baseline), full build ~2min wall; second cached run for the log file replays green with identical sync hash. Check 176 articles/152 fully proved (+3/+3 vs rung ten), audit clean (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 201 pages/156 nodes/552 code links (+3/+3/+3). Decision: negative ladder rung twelve ([-1]x14 at -1/14) follows the same factored pattern (extend the prelim list by one +3-arc crossing ⟨42,39,38,41,pos⟩ on fresh arcs 41/42); expect maxRecDepth 2048 pressure to persist. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twelve: ([-1]x14) inv carries -1/14 via factored preliminaries

Proved ([-1]x14) inv carries -1/14: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a35 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): fourteenth base crossing ⟨41,42,39,38,neg⟩ rejoins the prior rung's fresh arcs 39/38 per the add-gluing pattern (fresh arcs 41/42), switching to ⟨42,39,38,41,pos⟩; matrix ⟨col41,col42,col0,col3⟩, fraction (col42-col41)/(col42-col3). Same neg_ne_zero pattern as rungs one–eleven for the negative denominator multiple.
Depth setup (preemptive per standing orders): all three declarations start at `set_option maxRecDepth 2048` (placed before the docstrings) — required since rung ten, no new depth pressure at depth 14.
Two build iterations, both instructive, neither mathematical: the first failed on a generator bug that put a stray ` :` at the end of the universal's `hc` binder (the shared signature-suffix was correct for the prelim, which needs the colon, but wrong for a non-final binder) — Lean reported `unexpected token ':'` at the `hm` binder's end, which sent the diagnosis down a parser-depth rabbit hole (in-context bisect probes and standalone depth mimics, all exonerated depth: 15-deep terms parse fine, and the prelim with the identical expression compiled throughout). Diffing the universal's `hc` block against the prelim's byte-for-byte exposed the one-character defect; one-line fix, then green, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`; the two `admit` greps are English prose). Witness extends the rung-thirteen coloring by the forced pair (col41,col42)=(-12,-11), matrix ⟨-12,-11,2,3⟩. Spans checked upfront: prelim ~70 lines, universal ~120 lines, witness+existence ~146 lines, all under 200 namespace-aware — wired as a36/a36a/a36b.
Container split (honest, per the sub-chapter rule): adding the trio pushed neg-ladder/ to 26 articles, and audit flagged overfull-container (24-article limit), so rungs ten–fourteen (a32–a36, 15 factored-style nodes) moved into the new neg-ladder/factored-rungs/ sub-chapter with deepened Depends links (sibling prelim links unchanged; no external inbound links existed); neg-ladder/ keeps rungs three–nine plus one sub-chapter link (12 entries). Cost: repo-script WSL `lake build` green, DiagonalSumGeneral module 26s (24s at rung eleven; 10s at the a25 baseline), full build ~2min wall. Check 180 articles/155 fully proved (+4/+3 vs rung eleven — the extra article is the new sub-chapter index), audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 206 pages/159 nodes/555 code links (+5/+3/+3). Decision: negative ladder rung thirteen ([-1]x15 at -1/15) follows the same factored pattern into factored-rungs/ (9 node slots remain before the next split); watch the `hc`-binder colon on every new universal — the generator must use a colon-free suffix there. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung thirteen: ([-1]x15) inv carries -1/15 via factored preliminaries

Proved ([-1]x15) inv carries -1/15: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a36 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): fifteenth base crossing ⟨44,45,42,41,neg⟩ rejoins the prior rung's fresh arcs 42/41 per the add-gluing pattern (fresh arcs 44/45, +3 arc pattern), switching to ⟨45,42,41,44,pos⟩; matrix ⟨col44,col45,col0,col3⟩, fraction (col45-col44)/(col45-col3). Same neg_ne_zero pattern as rungs one–twelve for the negative denominator multiple.
Depth setup (preemptive per standing orders): all three declarations start at `set_option maxRecDepth 2048` (placed before the docstrings) — required since rung ten, no new depth pressure at depth 15.
Two process wrinkles, neither mathematical: (1) the hand-typed block came up short on paren runs at six of nine expression sites (hc-position first-lines took the +1 correctly, but the two `have hcs` first-lines and five nested first-lines were typed one–two short) — caught BEFORE building by the standing structural re-verification (per-site run/middle/add/crossing counts vs the rung-fourteen baseline: all nine sites now read run 16 / middles 13 / adds 14 / crossings 15, ALL-OK), then green first try, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`). The universal's `hc` binder was verified colon-free on re-read (the a36 stray-` :` class avoided). (2) the paren-fix script rewrote the whole file LF, flipping the repo's CRLF convention — detected via byte counts (`Coloring.lean`/JOURNAL all-CRLF) and restored to all-CRLF before building, so the working-tree diff stays purely additive. Witness extends the rung-fourteen coloring by the forced pair (col44,col45)=(-13,-12), matrix ⟨-13,-12,2,3⟩. Spans measured post-edit: prelim 76 lines, universal 124 lines, def 16 + existence 140 lines, all under 200 namespace-aware — wired as a37/a37a/a37b in factored-rungs/ from the start (container now 18 nodes; 6 slots remain before the next split). Cost: repo-script WSL `lake build` green first try, DiagonalSumGeneral module 28s (26s at rung twelve; 10s at the a25 baseline). Check 183 articles/158 fully proved (+3/+3), audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 209 pages/162 nodes/558 code links (+3/+3/+3). Decision: negative ladder rung fourteen ([-1]x16 at -1/16) follows the same factored pattern into factored-rungs/; keep the run-count script as a pre-build gate on every new rung (hand-typed paren runs are now a known fallible step). The negative-family induction question remains open.
## 2026-09-10 — Negative ladder rung fourteen: ([-1]x16) inv carries -1/16 via factored preliminaries

Proved ([-1]x16) inv carries -1/16: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a37 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): sixteenth base crossing ⟨47,48,45,44,neg⟩ rejoins the prior rung's fresh arcs 45/44 per the add-gluing pattern (fresh arcs 47/48, +3 arc pattern), switching to ⟨48,45,44,47,pos⟩; matrix ⟨col47,col48,col0,col3⟩, fraction (col48-col47)/(col48-col3). Same neg_ne_zero pattern as rungs one–thirteen for the negative denominator multiple.
Depth wrinkle (new this round): `set_option maxRecDepth 2048` — sufficient since rung ten — no longer suffices at depth 16: the universal's `hm`-typing step and the existence `rfl` matrix computation both hit maximum-recursion-depth errors, while the preliminary's `decide` still passes at 2048. Bumped exactly the two failing declarations to `set_option maxRecDepth 4096` (placed before the docstrings); prelim stays at 2048. Two build iterations: first failed only on the two depth errors (caught by Lean, fixed, re-read); then green, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`; the two `admit` greps are English prose).
Generation notes (no hand-typed paren runs this round): the rung-sixteen trio was derived from the rung-fifteen text by a mechanical transform script with asserted counts (9 expression sites now read run 17 / middles 14 / adds 15 / crossings 16, ALL-OK), which also caught the a36-class `hc`-colon check (universal verified colon-free). Re-reading still paid off: it caught two transform misses the gates did not cover — a spaced `-1 / 15` in the universal `hval` (the replace only covered the spaceless form) and a dangling `mpr h15` binder after the h16 renames — both fixed before building. Witness extends the rung-fifteen coloring by the forced pair (col47,col48)=(-14,-13), matrix ⟨-14,-13,2,3⟩. Spans measured post-edit: prelim 80 lines, universal 128 lines, def 19 + existence 147 lines, all under 200 namespace-aware — wired as a38/a38a/a38b in factored-rungs/ from the start (container now 21 nodes; 3 slots remain before the next split). Blueprint depth flags updated honestly (prelim 2048, universal/existence 4096).
Cost: repo-script WSL `lake build` green on the second iteration, DiagonalSumGeneral module 30s (28s at rung thirteen; 10s at the a25 baseline), full build ~51s wall. Check 186 articles/161 fully proved (+3/+3), audit clean (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 212 pages/165 nodes/561 code links (+3/+3/+3). Decision: negative ladder rung fifteen ([-1]x17 at -1/17) follows the same factored pattern into factored-rungs/; expect maxRecDepth 4096 pressure to persist and watch for spaced-fraction/dangling-binder transform misses in review. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung fifteen: ([-1]x17) inv carries -1/17 via factored preliminaries

Proved ([-1]x17) inv carries -1/17: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a38 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): seventeenth base crossing ⟨50,51,48,47,neg⟩ rejoins the prior rung's fresh arcs 48/47 per the add-gluing pattern (fresh arcs 50/51, +3 arc pattern), switching to ⟨51,48,47,50,pos⟩; matrix ⟨col50,col51,col0,col3⟩, fraction (col51-col50)/(col51-col3). Same neg_ne_zero pattern as rungs one–fourteen for the negative denominator multiple.
Depth setup (preemptive per standing orders): all three declarations start at rung-sixteen depths from the start — prelim `set_option maxRecDepth 2048`, universal and existence `set_option maxRecDepth 4096` (all placed before the docstrings) — and all three elaborated first try at depth 17, no new depth pressure observed.
Generation notes: the rung-seventeen trio was derived from the rung-sixteen text by a mechanical transform script with asserted counts, corrected mid-flight on gate failures that mapped the true structure — 9 expression sites (not 7: the universal conclusion and the existence header carry the invert expression too), tail lines at two indents (the universal `hc` tail is colon-free as a non-final binder, the a36 class again), 8 `col 48 - col 3` lines (not 6), 5 `col 48 - col 47` lines (not 4), `Rat.divInt` in both `show` and `hdiv`, docstring `[-1]` chains on 3 sites (the prelim docstring carries none). Re-reading still paid off with three misses the gates did not cover — prose `-16d` in the universal docstring, the ascribed `(16 : Int)` in the `h17` decl (the `16 *` replace missed the ascribed form), and the bare `exact_mod_cast h16n` use (the rename only covered the `h16n :` decl) — all fixed before building. Witness extends the rung-sixteen coloring by the forced pair (col50,col51)=(-15,-14), matrix ⟨-15,-14,2,3⟩. Spans measured post-edit: prelim 85 lines, universal 133 lines, def 17 + existence 155 lines, all under 200 namespace-aware — wired as a39/a39a/a39b in factored-rungs/ from the start (container now 24 nodes: AT the 24-article limit, no split this round — the next rung must create the next sub-chapter per the established pattern). Blueprint depth flags honest (prelim 2048, universal/existence 4096).
Cost: repo-script WSL `lake build` green first try, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`; both `admit` greps are English prose); DiagonalSumGeneral module 32s (30s at rung fourteen; 10s at the a25 baseline). Check 189 articles/164 fully proved (+3/+3), audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 215 pages/168 nodes/564 code links (+3/+3/+3). Decision: negative ladder rung sixteen ([-1]x18 at -1/18) is next but cannot go into factored-rungs/ (full at 24) — create the next sub-chapter per the a36 pattern first, then follow the same factored pattern with preemptive 2048/4096/4096 depths; extend the transform script's gate list with this round's corrections (9 sites, two indents, ascribed-int and bare-h16n variants). The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung sixteen: ([-1]x18) inv carries -1/18 via factored preliminaries

Proved ([-1]x18) inv carries -1/18: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a39 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): eighteenth base crossing <53,54,51,50,neg> rejoins the prior rung's fresh arcs 51/50 per the add-gluing pattern (fresh arcs 53/54, +3 arc pattern), switching to <54,51,50,53,pos>; matrix <col53,col54,col0,col3>, fraction (col54-col53)/(col54-col3). Same neg_ne_zero pattern as rungs one–fifteen for the negative denominator multiple.
Container first (honest, per the sub-chapter rule): factored-rungs/ was AT the 24-article limit, so the next sub-chapter neg-ladder/factored-rungs-two/ was created BEFORE wiring (README + parent-ladder link; nothing moved — new rungs go in the new container); the a40 trio is its first content (container now 4 nodes: index + 3).
Depth setup (per standing orders, adjusted): all three declarations start at set_option maxRecDepth 4096 from the start (placed before the docstrings) — the prelim was preemptively bumped from its rung-seventeen 2048 since depth-18 is strictly larger and the universal/existence already needed 4096 since rung fourteen; all three elaborated first try at depth 18, no new depth pressure observed.
Generation notes: the rung-eighteen trio was derived from the rung-seventeen text by a mechanical transform script with asserted counts (9 expression sites now read run 19 / middles 16 / adds 17 / crossings 18, ALL-OK; hnumR/hdenR ascribed sites corrected 3->4 mid-flight on the gate failure — the hfrac show RHS carries them too; the hc-colon gate was scoped to the universal after it tripped on the prelim's legitimate signature colon; the adds gate was fixed to count lead+middle .add lines). Re-reading still paid off with three misses the gates did not cover — a dangling exact_mod_cast hden17 in the hdenR proof (decl and rw-use were renamed), the bare use exact hmRw <c5051, c513> after the c5354/c543 decl renames, and the bare show condition if col 51 - col 3 = 0 (ascribed and divInt forms were renamed) — all fixed before building. Witness extends the rung-seventeen coloring by the forced pair (col53,col54)=(-16,-15), matrix <-16,-15,2,3>. Spans measured post-edit: prelim 91 lines, universal 139 lines, def 18 + existence 163 lines, all under 200 namespace-aware — wired as a40/a40a/a40b in factored-rungs-two/ from the start. Blueprint depth flags honest (all three 4096).
Cost: repo-script WSL lake build green first try, 1005 jobs, zero sorry/admit/axiom (the five repo-wide admit greps are English prose), no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 hok1); DiagonalSumGeneral module 36s (32s at rung fifteen; 10s at the a25 baseline). Check 193 articles/167 fully proved (+4/+3 — the extra article is the new sub-chapter index), audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 220 pages/171 nodes/567 code links (+5/+3/+3). Decision: negative ladder rung seventeen ([-1]x19 at -1/19) follows the same factored pattern into factored-rungs-two/ (20 node slots remain before the next split); extend the transform gate list with this round's corrections (hfrac-show-RHS ascribed pair, exact_mod_cast hden-use, hmRw use-site, bare show-if condition). The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung seventeen: ([-1]x19) inv carries -1/19 via factored preliminaries

Proved ([-1]x19) inv carries -1/19: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a40 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): nineteenth base crossing <56,57,54,53,neg> rejoins the prior rung's fresh arcs 54/53 per the add-gluing pattern (fresh arcs 56/57, +3 arc pattern), switching to <57,54,53,56,pos>; matrix <col56,col57,col0,col3>, fraction (col57-col56)/(col57-col3). Same neg_ne_zero pattern as rungs one to sixteen for the negative denominator multiple.
Depth setup (per standing orders): all three declarations start at set_option maxRecDepth 4096 from the start (placed before the docstrings) — required since rung sixteen, no new depth pressure at depth 19.
Generation notes: the rung-nineteen trio was derived from the rung-eighteen text by a mechanical transform script with asserted counts (9 expression sites now read run 20 / middles 17 / adds 18 / crossings 19, ALL-OK), carrying forward all prior gate corrections. New gate corrections this round, all caught pre-write by the gates themselves: the first-line detector missed binder prefixes like (hc : ((... (needs a substring search, not a rooted match); the final-line gate missed the two .invert.crossings tails (match .invert, not .invert)); the paren-run measurement via a repeated capture group returns only the last iteration (measure the whole match minus the token instead); the spaceless-fraction count is 7 not 4 (three docstring/show sites forgotten); the neg-multiple count is 4 not 3 (the h19ne line carries it too); the middle-.add gate is exactly 17 per 9 sites (first-line runs are not bare middle lines); the NegNineteen stale-guard is bogus (sharpColAddNegNineteen legitimately contains it). Re-reading still paid off as the structural backstop: the full new block (429 lines) was re-read post-edit — prelim 19-rule conclusion / 19-crossing list / 19 exact lines, universal r19 / e19 / hmRw / hnum / hden19 / c5657 / c573 / h19-triple / -1/19 with colon-free hc, existence witness cells 56/57 + matrix <-17,-16,2,3> + 19 rfls + 19 show blocks including the 57-block + (-16) show-if/hdiv — all confirmed, no misses this round. Hand-verified PD code: switched <57,54,53,56,pos> is (b,c,d,a) of base <56,57,54,53,neg> per the mirror-switch pattern; witness rule -16 = -16 and -15 + -17 = 2 * -16; fraction 1 / -19. Witness extends the rung-eighteen coloring by the forced pair (col56,col57)=(-17,-16), matrix <-17,-16,2,3>. Spans measured post-edit: prelim 94 lines, universal 140 lines, def 18 + existence 168 lines, all under 200 namespace-aware — wired as a41/a41a/a41b in factored-rungs-two/ from the start (container now 7 nodes). Blueprint depth flags honest (all three 4096).
Cost: repo-script WSL lake build green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 hok1); DiagonalSumGeneral module 39s (36s at rung sixteen; 10s at the a25 baseline). Check 196 articles/170 fully proved (+3/+3), audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 223 pages/174 nodes/570 code links (+3/+3/+3). Decision: negative ladder rung eighteen ([-1]x20 at -1/20) follows the same factored pattern into factored-rungs-two/ (17 node slots remain before the next split); the transform gate list now covers binder-prefix runs, crossings-tails, whole-match measurement, 7x fractions, 4x neg-multiples, and drops the NegNineteen guard. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung eighteen: ([-1]x20) inv carries -1/20 via factored preliminaries

Proved ([-1]x20) inv carries -1/20: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a41 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twentieth base crossing <59,60,57,56,neg> rejoins the prior rung's fresh arcs 57/56 per the add-gluing pattern (fresh arcs 59/60, +3 arc pattern), switching to <60,57,56,59,pos>; matrix <col59,col60,col0,col3>, fraction (col60-col59)/(col60-col3). Same neg_ne_zero pattern as rungs one to seventeen for the negative denominator multiple.
Depth setup (per standing orders): all three declarations start at set_option maxRecDepth 4096 from the start (placed before the docstrings) — required since rung sixteen, no new depth pressure at depth 20.
Generation notes: the rung-twenty trio was derived from the rung-nineteen text by a mechanical transform script with asserted gates (9 expression sites now read run 21 / middles 18 / adds 19 / crossings 20, ALL-OK), carrying forward all prior corrections. New gate corrections this round, all caught pre-write: middle-.add lines carry two indents (6-space top-level sites, 8-space nested sites), so the gate counts stripped lines (153 = 17 x 9) and the insertion duplicates the line following each first-line indent-aware (+9 = 162); h19n counting must subtract the h19ne carriers (2 bare + 2 carried); the r20 post-gate is >= 2 not >= 3 (obtain-list + decl-use only). Two real defects caught post-write by the backstops: the rcases append landed after the line's carriage return (bare-LF line — the byte-count/CRLF gate fired; one-byte fix, file all-CRLF re-verified) and prose -19d in the universal docstring (the classic class; fixed). Blueprint misses caught by re-reading + check-status forensics: the prelim frontmatter ended up with a duplicated lean line and no proof: formalized (first check showed +3 articles but only +1 fully proved — per-node status dump showed prelim at can_prove and universal at proved; restored the flag, re-check +3/+3), and the universal Depends link pointed at a41-neg-twenty (fixed to a42). Re-reading confirmed the full new block (449 lines): prelim 20-rule conclusion / 20-crossing list / 20 exact lines, universal r20 / e20 / hmRw / hnum / hden20 / c5960 / c603 / h20-triple / -1/20 with colon-free hc, existence witness cells 59/60 + matrix <-18,-17,2,3> + 20 rfls + 20 show blocks including the 60-block + (-17) show-if/hdiv. Hand-verified PD code: switched <60,57,56,59,pos> is (b,c,d,a) of base <59,60,57,56,neg> per the mirror-switch pattern; witness rule -17 = -17 and -16 + -18 = 2 * -17; fraction 1 / -20. Witness extends the rung-nineteen coloring by the forced pair (col59,col60)=(-18,-17), matrix <-18,-17,2,3>. Spans measured post-edit: prelim 100 lines, universal 148 lines, def 17 + existence 179 lines, all under 200 namespace-aware — wired as a42/a42a/a42b in factored-rungs-two/ from the start (container now 10 nodes). Blueprint depth flags honest (all three 4096).
Cost: repo-script WSL lake build green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 hok1); DiagonalSumGeneral module 41s (39s at rung seventeen; 10s at the a25 baseline). Check 199 articles/173 fully proved (+3/+3), audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 226 pages/177 nodes/573 code links (+3/+3/+3). Decision: negative ladder rung nineteen ([-1]x21 at -1/21) follows the same factored pattern into factored-rungs-two/ (14 node slots remain before the next split); the transform gate list now covers stripped middle counts with indent-aware duplication, h19n-minus-h19ne counting, and r20 >= 2; frontmatter checks (no duplicated lean keys, proof flag present, Depends links point at the new rung) join the pre-check gate list. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung nineteen: ([-1]x21) inv carries -1/21 via factored preliminaries

Proved ([-1]x21) inv carries -1/21: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a42 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twenty-first base crossing ⟨62,63,60,59,neg⟩ rejoins the prior rung's fresh arcs 59/60 per the add-gluing pattern (fresh arcs 62/63, +3 arc pattern), switching to ⟨63,60,59,62,pos⟩; matrix ⟨col62,col63,col0,col3⟩, fraction (col63-col62)/(col63-col3). Same neg_ne_zero pattern as rungs one–eighteen for the negative denominator multiple.
Depth setup (per standing orders): all three declarations start at `set_option maxRecDepth 4096` from the start (placed before the docstrings) — required since rung sixteen, no new depth pressure at depth 21.
Generation notes: the rung-twenty-one trio was derived from the rung-twenty text by a mechanical transform script with asserted gates (9 expression sites now read run 22 / middles 19 / adds 20 / crossings 21, ALL-OK), carrying forward all prior corrections. One gate correction this round: the c-decl updates must rename the full equation (`have c6263 : col 62 = col 63`), not just the hypothesis name — the post-gate asserting zero `col 59 = col 60` caught the name-only form pre-write. Re-reading confirmed the full new block (466 lines): prelim 21-rule conclusion / 21-crossing list / 21 exact lines, universal r21 / e21 / hmRw / hnum / hden21 / c6263 / c633 / h21-triple / -1/21 with colon-free hc, existence witness cells 62/63 + matrix ⟨-19,-18,2,3⟩ + 21 rfls + 21 show blocks including the 63-block + (-18) show-if/hdiv. Hand-verified PD code: switched ⟨63,60,59,62,pos⟩ is (b,c,d,a) of base ⟨62,63,60,59,neg⟩ per the mirror-switch pattern; witness rule -18 = -18 and -17 + -19 = 2 * -18; fraction 1 / -21. Witness extends the rung-twenty coloring by the forced pair (col62,col63)=(-19,-18), matrix ⟨-19,-18,2,3⟩. Spans measured post-edit: prelim 106 lines, universal 172 lines, def 18 + existence 170 lines, all under 200 namespace-aware — wired as a43/a43a/a43b in factored-rungs-two/ from the start (container now 13 nodes; 11 slots remain before the next split). Blueprint depth flags honest (all three 4096).
Process incident (recovered, no mathematics lost): the first splice REPLACED the rung-twenty block instead of appending it (the new tree dropped the old block — 7792 lines instead of 8240). The Windows re-check caught it immediately (a42 trio `declaration not found in src`, otherwise +3/+3). Recovery: reverse-transformed the new block back to rung-twenty text with inverted asserted gates — the reconstruction re-passed every forward pre-gate (runs 21, middles 162, .add 171, crossings 60, negOne 80, [-1] 60, rcases 20, spans 101/167/180) — then re-spliced as prefix + old + new + end (8240 lines, all-CRLF), re-verified all six declarations present exactly once each plus both junctions by re-reading, and rebuilt. New standing gate: every splice script must assert post-write that the PRIOR rung's declaration names are still present (retention check), not just that the new block is well-formed.
 Cost: repo-script WSL `lake build` green after the re-splice (the earlier green build on the replaced tree is discarded), 1005 jobs, zero sorry/admit/axiom (the two `admit` greps are English prose), no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`); DiagonalSumGeneral module 45s (41s at rung eighteen; 10s at the a25 baseline). Check 202 articles/176 fully proved (+3/+3), 178 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 234 pages/180 nodes/576 code links (+8/+3/+3 — pages include per-node dependency views). Decision: negative ladder rung twenty ([-1]x22 at -1/22) follows the same factored pattern into factored-rungs-two/; size warning: def+existence span is now 188/200 (universal 172) — rung twenty-two projects ~196 and rung twenty-three will exceed, so plan a size strategy (split the existence witness from the fraction computation, or factor the rfl/show blocks) within two rungs. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twenty: ([-1]x22) inv carries -1/22 via factored preliminaries with split witness node

Proved ([-1]x22) inv carries -1/22: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a43 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twenty-second base crossing ⟨65,66,63,62,neg⟩ rejoins the prior rung's fresh arcs 63/62 per the add-gluing pattern (fresh arcs 65/66, +3 arc pattern), switching to ⟨66,63,62,65,pos⟩; matrix ⟨col65,col66,col0,col3⟩, fraction (col66-col65)/(col66-col3). Same neg_ne_zero pattern as rungs one–nineteen for the negative denominator multiple.
Depth setup (per standing orders): all three proof declarations start at `set_option maxRecDepth 4096` from the start (placed before the docstrings) — required since rung sixteen, no new depth pressure at depth 22; the witness def is a plain if-else chain and carries no override.
Size plan executed (as mandated last round): def+existence projected ~196/200, so the existence witness was split from the fraction computation up front — the witness def `sharpColAddNegTwentyTwo` is claimed in its own node a44b (declaration: def, no proof key per def-node precedent), and the fraction computation in a44c Depends on it honestly. Raw spans post-edit: prelim 110 lines (8240–8349), universal 158 (8351–8508), def 17 (8510–8526), existence 195 (8528–8722); audit passed with no node-too-large, so every claim holds within limits. Wired as a44/a44a/a44b/a44c in factored-rungs-two/ from the start (container now 17 nodes; 7 slots remain before the next split). Blueprint depth flags honest (prelim/universal/existence 4096; def none needed).
Generation notes: the rung-twenty-two quartet was derived from the rung-twenty-one text by a mechanical transform script with asserted gates (9 expression sites now read run 23 / middles 180 / tails 9, ALL-OK), carrying forward all prior corrections. Two defects this round, both caught before the green build: (1) the gates never covered docstring `[-1]` chains (3 sites: universal/def/existence docstrings; prelim carries none) — re-reading caught all three still at 21 links, fixed by a scoped new-block-only patch (fold the chain gate into the script from the start next round); (2) a NEW spurious step added a third close paren to the nine `.invert` tails, failing the first build with parse errors at the prelim-hc, universal-hc, and existence-header tails (`unexpected token '.'`, `unexpected token ')'`) — balance forensics showed the +1 first-line open and +1 middle cancel exactly, so tails must keep their two closes (new-block nets [0,0,0,-2,-1,-1,0,0,0] now match rung twenty-one exactly); reverted scoped to the new block, then green. The retention gate (prior-rung names terminator-aware, since each 21-name is a prefix of its 22-name) and the all-CRLF byte gate both held; the line-count gate caught one splice-slice bug pre-write (append, not replace — the a43 incident class, avoided). Witness extends the rung-twenty-one coloring by the forced pair (col65,col66)=(-20,-19), matrix ⟨-20,-19,2,3⟩. Hand-verified PD code: switched ⟨66,63,62,65,pos⟩ is (b,c,d,a) of base ⟨65,66,63,62,neg⟩ per the mirror-switch pattern; witness rule -19 = -19 and -18 + -20 = 2 * -19; fraction 1 / -22. Re-reading confirmed the full new block (484 lines): prelim 22-rule conclusion / 22-crossing list / 22 exact lines, universal r22 / e22 / hmRw / hnum / hden22 / c6566 / c663 / h22-triple / -1/22 with colon-free hc, existence witness cells 65/66 + 22 rfls + 22 show blocks including the 66-block + (-19) show-if/hdiv.
Cost: repo-script WSL `lake build` green on the second iteration (parse-error build discarded), 1005 jobs, zero sorry/admit/axiom in the new block (the two repo-wide `admit` greps are English prose), no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`); DiagonalSumGeneral module 51s (45s at rung nineteen; 10s at the a25 baseline). Check 206 articles/180 fully proved (+4/+4 — the def node resolves by elaboration), 182 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 233 pages/184 nodes/580 code links (+4 nodes/+4 links; pages -1 vs a43 with all four dep-views and structure/full.md entries verified present — unexplained benign render-accounting variance, exit 0). Decision: negative ladder rung twenty-one ([-1]x23 at -1/23) follows the same factored pattern into factored-rungs-two/ with the witness split retained (fold the [-1]-chain gate into the transform from the start; never touch tails); expect maxRecDepth 4096 pressure to persist. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twenty-one: ([-1]x23) inv carries -1/23 via factored preliminaries with split witness node

Proved ([-1]x23) inv carries -1/23: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a44 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twenty-third base crossing ⟨68,69,66,65,neg⟩ rejoins the prior rung's fresh arcs 66/65 per the add-gluing pattern (fresh arcs 68/69, +3 arc pattern), switching to ⟨69,66,65,68,pos⟩; matrix ⟨col68,col69,col0,col3⟩, fraction (col69-col68)/(col69-col3). Same neg_ne_zero pattern as rungs one–twenty for the negative denominator multiple.
Depth setup (per standing orders): all three proof declarations start at `set_option maxRecDepth 4096` from the start (placed before the docstrings) — required since rung sixteen, no new depth pressure at depth 23; the witness def is a plain if-else chain and carries no override.
Size plan executed (span check first, as mandated): the audit-style span gate in the transform script projected the existence at ~202/200 in the old 4-line `show` form, so all twenty-three `show` blocks were tightened to 3-line form up front — the `B = 2 * C` continuation joined onto one line, whitespace-only, identical proof tokens — holding the existence at 180 with margin. Raw spans post-edit (audit-style, decl-line to next-decl-line): prelim 117, universal 161, def 19, existence 180 — all under 200, namespace-aware. Wired as a45/a45a/a45b/a45c in factored-rungs-two/ from the start (container now 21 nodes; 3 slots remain before the next split). Blueprint depth flags honest (prelim/universal/existence 4096; def none needed); existence node carries a size note documenting the 3-line form and its rationale.
Generation notes: the rung-twenty-three quartet was derived from the rung-twenty-two text by a mechanical transform script with asserted gates (9 expression sites now read run 24 / middles 189 / tails 9-modulo-hM, ALL-OK), carrying forward all prior corrections with the [-1]-chain gate folded in from the start (3 docstring sites at 23 links, 69 total — no re-reading rescue needed this round). Three script-gate bugs, all caught pre-write by the gates themselves, none mathematical: (1) the tail-identity gate tripped on the hM line, whose witness-name update (TwentyTwo→TwentyThree) is the one legitimate tail diff — gate now normalizes that name and asserts the single diff is the hM line; (2) the chain gate asserted zero 22-link chains post-transform, but Q22 is a substring of Q23 (3 hits, one per new chain) — gate now asserts 3x23-chains plus 69 total links; (3) the span gate looked up namespace-qualified names in an unqualified index — gate now tracks namespace open/close like the real audit scanner. Re-reading still served as the structural backstop and confirmed the full new block (478 lines: prelim 23-rule conclusion / 23-crossing list / 23 exact lines, universal r23 / e23 / hmRw / hnum / hden23 / c6869 / c693 / h23-triple / -1/23 with colon-free hc, def cells 68/69, existence 23 rfls + 23 joined show blocks including the 69-block + (-20) show-if/hdiv) with no misses. Witness extends the rung-twenty-two coloring by the forced pair (col68,col69)=(-21,-20), matrix ⟨-21,-20,2,3⟩. Hand-verified PD code: switched ⟨69,66,65,68,pos⟩ is (b,c,d,a) of base ⟨68,69,66,65,neg⟩ per the mirror-switch pattern; witness rule -20 = -20 and -19 + -21 = 2 * -20; fraction 1 / -23.
Cost: repo-script WSL `lake build` green first try, 1005 jobs, zero sorry/admit/axiom in the new block (the two repo-wide `admit` greps are English prose), no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`); DiagonalSumGeneral module 54s (51s at rung twenty; 10s at the a25 baseline), full build 76.3s wall. Check 210 articles/184 fully proved (+4/+4 — the def node resolves by elaboration), 186 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 237 pages/188 nodes/584 code links (+4/+4/+4). Decision: negative ladder rung twenty-two ([-1]x24 at -1/24) CANNOT go into factored-rungs-two/ as a quartet (21 + 4 = 25 > 24 — create the next sub-chapter per the a36/a40 pattern first), then follow the same factored pattern with preemptive 4096 depths, witness split retained, and 3-line show form throughout (existence projects ~187); the gate corrections above (hM-normalized tails, substring-aware chains, namespace-tracking spans) carry forward. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twenty-two: ([-1]x24) inv carries -1/24 via factored preliminaries with split witness node

Proved ([-1]x24) inv carries -1/24: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a45 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twenty-fourth base crossing ⟨71,72,69,68,neg⟩ rejoins the prior rung's fresh arcs 69/68 per the add-gluing pattern (fresh arcs 71/72, +3 arc pattern), switching to ⟨72,69,68,71,pos⟩; matrix ⟨col71,col72,col0,col3⟩, fraction (col72-col71)/(col72-col3). Same neg_ne_zero pattern as rungs one–twenty-one for the negative denominator multiple.
Container first (honest, per the sub-chapter rule): factored-rungs-two/ was at 21 nodes and a quartet cannot fit (21 + 4 = 25 > 24), so the next sub-chapter neg-ladder/factored-rungs-three/ was created BEFORE wiring (README + parent-ladder link; nothing moved — new rungs go in the new container, now 5 nodes: index + quartet); the container README states the 21+4 rationale, not the old at-limit wording.
Depth setup (adjusted after build feedback): all three proof declarations started at `set_option maxRecDepth 4096` (placed before the docstrings) per standing orders, but depth 24 exceeds 4096 in two places — the universal's `hm`-typing step (line 9431) and the existence `rfl` matrix computation (line 9608), the same two sites that forced the 2048→4096 bumps at rungs eleven/fourteen — so exactly those two declarations were bumped to `set_option maxRecDepth 8192`; the prelim `decide` still elaborates at 4096. The witness def is a plain if-else chain and carries no override. Blueprint depth flags honest (prelim 4096, universal/existence 8192; def none).
Size plan executed (span check first, as mandated): 3-line `show` form retained throughout; raw spans post-edit (audit-style, decl-line to next-decl-line): prelim 118, universal 166, def 17, existence 185 — all under 200, namespace-aware. Wired as a46/a46a/a46b/a46c in factored-rungs-three/ from the start; existence node size note updated honestly (joined form holds ~185; the ~202 4-line-form projection is kept as history).
Generation notes: the rung-twenty-four quartet was derived from the rung-twenty-three text by a mechanical transform script with asserted gates (9 expression sites now read run 25 / middles 198 / tails 9-modulo-hM, ALL-OK), carrying forward all prior corrections. Script-gate bugs, all caught pre-write: the first-line run probe via repeated capture group returns only the last iteration (measure whole-match minus token); theorem-name chains need the `negOne(_negOne){22}` regex form (prefix-underscore construction double-counts the separator); node prose needed the same number updates as Lean (`-23d`, `1/-23`, switched-rules/count lines the generator initially missed — re-reading caught `-23d`, fixed). Structural re-reading confirmed the full new block (495 lines: prelim 24-rule conclusion / 24-crossing list / 24 exact lines, universal r24 / e24 / hmRw / hnum / hden24 / c7172 / c723 / h24-triple / -1/24 with colon-free hc, def cells 71/72, existence 24 rfls + 24 joined show blocks including the 72-block + (-21) show-if/hdiv) with no further misses. Witness extends the rung-twenty-three coloring by the forced pair (col71,col72)=(-22,-21), matrix ⟨-22,-21,2,3⟩. Hand-verified PD code: switched ⟨72,69,68,71,pos⟩ is (b,c,d,a) of base ⟨71,72,69,68,neg⟩ per the mirror-switch pattern; witness rule -21 = -21 and -20 + -22 = 2 * -21; fraction 1 / -24.
Cost: repo-script WSL `lake build` green on the second iteration (depth-error build discarded), 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`); DiagonalSumGeneral module 57s (54s at rung twenty-one; 10s at the a25 baseline), full log build-2026-09-10-a46.log. Check 215 articles/188 fully proved (+5/+4 — the extra article is the new sub-chapter index; the def node resolves by elaboration), 190 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 243 pages/192 nodes/588 code links (+6/+4/+4 — pages include the new index plus per-node dependency views). Decision: negative ladder rung twenty-three ([-1]x25 at -1/25) follows the same factored pattern into factored-rungs-three/ (19 node slots remain) with preemptive 8192 depths on universal/existence (4096 prelim), witness split + 3-line show form retained; size warning: existence span is now 185/200 — rung twenty-five projects ~192 and rung twenty-six will exceed, so plan the next size strategy (split the existence fraction computation from the rule discharges, or factor the show blocks) within two rungs. The negative-family induction question remains open.




## 2026-09-10 — Negative ladder rung twenty-three: ([-1]x25) inv carries -1/25 via factored preliminaries with split witness node

Proved ([-1]x25) inv carries -1/25: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a46 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twenty-fifth base crossing <74,75,72,71,neg> rejoins the prior rung's fresh arcs 72/71 per the add-gluing pattern (fresh arcs 74/75, +3 arc pattern), switching to <75,72,71,74,pos>; matrix <col74,col75,col0,col3>, fraction (col75-col74)/(col75-col3). Same neg_ne_zero pattern as rungs one to twenty-two for the negative denominator multiple.
Depth setup (per standing orders): prelim set_option maxRecDepth 4096, universal and existence set_option maxRecDepth 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all three elaborated first try at depth 25, no new depth pressure observed. The witness def is a plain if-else chain and carries no override. Blueprint depth flags honest (prelim 4096, universal/existence 8192; def none).
Generation notes: the rung-twenty-five quartet was derived from the rung-twenty-four text by a mechanical transform script with asserted gates (9 expression sites now read run 26 / middles 207 / tails 9-modulo-hM, ALL-OK), carrying forward all prior corrections. Two gate bugs, both caught pre-write by the gates themselves, none mathematical: (1) the P residual-24 gate counted lines but was seeded with the occurrence count (6 lines vs 7 occurrences — the col-24 rule line carries two) — gate now asserts 6 lines pre and post; (2) the U no-r24/e24 gate forgot the prefix obtain lines legitimately retain r24/e24a/e24b (the rung-n variables stay referenced) — gate now asserts exactly 2 residual 24-lines are those obtains, plus r25/e25 == 2 positive. Structural re-reading confirmed junctions (rung-24 tail into new prelim set_option), the def tail cells 74/75 = (-23,-22), the 3-line 75-block, the (-22) show-if/hdiv with -1/25, and the file tail (rw, blank, end). Witness extends the rung-twenty-four coloring by the forced pair (col74,col75)=(-23,-22), matrix <-23,-22,2,3>. Hand-verified PD code: switched <75,72,71,74,pos> is (b,c,d,a) of base <74,75,72,71,neg> per the mirror-switch pattern; witness rule -22 = -22 and -21 + -23 = 2 * -22; fraction 1 / -25. Spans measured post-edit: prelim 127, universal 171, def 19, existence 194, all under 200 namespace-aware — wired as a47/a47a/a47b/a47c in factored-rungs-three/ from the start (container now 9 nodes; 15 slots remain before the next split).
Cost: repo-script WSL lake build green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 hok1); DiagonalSumGeneral module 68s (57s at rung twenty-two; 10s at the a25 baseline), full log build-2026-09-10-a47.log. Check 219 articles/192 fully proved (+4/+4), 194 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 247 pages/196 nodes/592 code links (+4/+4/+4). Process note: the first render attempt hit the 300s tool timeout mid-progress (site-src was still growing); a retry with a longer timeout completed — budget 10+ min for render from here on.
RUNG-TWENTY-SIX SIZE DESIGN (recorded, NOT implemented): existence span is now 194/200 and grows ~7-9/rung, so rung twenty-six ([-1]x26) projects ~201-203 and exceeds the limit in its current shape. Design adopted for that round: add a second factored preliminary for the witness rule discharges — a new theorem over the witness coloring stating the 26 arc-equations (proved by the 26 decide pairs + rcases, ~100 lines), claimed in its own node beside the crossings prelim; the existence node then Depends on it and keeps only the hcs-list decide, hM rfl, NotMono decide, and fraction normalization (~110 lines). Rationale: extending the existing crossings prelim is rejected (127 + ~95 would itself exceed 200); splitting the universal is unnecessary (171/200, growth ~5-6/rung, fine until ~rung 29 — flagged horizon: a universal-side split or tighter linarith packaging will be needed by rung 29); the crossings prelim (growth ~5/rung) is fine to ~rung 39 and the new witness-rules node (growth ~7/rung from ~100) to ~rung 39, so this one split carries the ladder to ~rung 28 with no further structural changes. Implement it during the rung-twenty-six round, not before. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twenty-four: ([-1]x26) inv carries -1/26 via factored preliminaries with witness-rules split

Proved ([-1]x26) inv carries -1/26: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a47 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twenty-sixth base crossing ⟨77,78,75,74,neg⟩ rejoins the prior rung's fresh arcs 75/74 per the add-gluing pattern (fresh arcs 77/78, +3 arc pattern), switching to ⟨78,75,74,77,pos⟩; matrix ⟨col77,col78,col0,col3⟩, fraction (col78-col77)/(col78-col3). Same neg_ne_zero pattern as rungs one to twenty-three for the negative denominator multiple.
Size design executed (as journaled last round, now implemented): the existence projected ~201-203 in the old shape, so a second factored preliminary carries the witness rule discharges — new theorem `sharpColAddNegTwentySix_witness_rules` stating the 26 arc-equations (proved by 26 decide pairs, 83 lines), claimed in its own node a48c beside the crossings prelim; the existence node obtains `w1 … w26` from it and applies one line per crossing (`exact wN`, no `show` blocks), holding existence at 151 lines. Raw spans post-edit (audit-style): prelim 130, universal 178, def 17, witness-rules 83, existence 151 — all under 200, namespace-aware. Wired as a48/a48a/a48b/a48c/a48d in factored-rungs-three/ from the start (container now 14 nodes; 10 slots remain before the next split). Blueprint depth flags honest (crossings prelim 4096, witness-rules 4096, universal/existence 8192; def none).
Depth setup (per standing orders): prelim 4096, universal and existence 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all five elaborated first try at depth 26, no new depth pressure observed. The witness def needs no override (plain if-else chain); the rules node carries 4096 matching the crossings prelim.
Generation notes: the rung-twenty-six quintet was derived from the rung-twenty-five text by a mechanical transform script with asserted gates (9 expression sites now read run 27 / middles 216 / crossings 234, ALL-OK), carrying forward all prior corrections. Gate calibrations caught pre-write, none mathematical: the `.invert` tail matcher must accept `.invert.crossings` (the a41 class, re-met); the Lean prelim docstring is 2 lines (crossing detail lives in the blueprint node, unlike the universal docstring which does carry `twenty-five switched`); the hden-decl rename precedes hden25 counting (2 remain, not 3); the universal conclusion carries a second spaceless `-1/25` (docstring + conclusion = 2, hval spaced = 1); `mpr h25` needs its own rename; `25 * (col 2 - col 1)` is 3 sites (hdenR + h25ne + h25n); the existence hdiv repeats the divInt args and the header a third `(-1/25 : Rat)` (counts 2/2/3); the existence docstring value `carries value -1/25.` needs its own replace; the rung-26 existence name replace must target the `negOne :` tail (the decl line wraps the count-sensitive middle). Two representation fixes: multi-line replacements re-split into separate list elements (glued lines undercount line gates — this masked the conclusion count by one), and block-level `col 75` residual gating is dropped (arc 75 is legitimate in rules 25-26; only the stale difference/equation forms are gated, plus universal-level zero). Structural re-reading confirmed junctions (rung-25 tail into new prelim set_option), prelim 26-rule conclusion / 26-crossing list / 26 exact lines, universal r26 / e26 / hmRw / hnum / hden26 / c7778 / c783 / h26-triple / -1/26 with colon-free hc, def tail cells 77/78 = (-24,-23), witness-rules 26 conjuncts + 26 decide-pairs with the 26th rule cross-checked against the prelim conclusion mapping, existence 26-crossing hcs + ⟨-24,-23,2,3⟩ rfl + 26 one-line exacts + (-23) show-if/hdiv with -1/26, and the file tail (rw, blank, end). Witness extends the rung-twenty-five coloring by the forced pair (col77,col78)=(-24,-23), matrix ⟨-24,-23,2,3⟩. Hand-verified PD code: switched ⟨78,75,74,77,pos⟩ is (b,c,d,a) of base ⟨77,78,75,74,neg⟩ per the mirror-switch pattern; witness rule -23 = -23 and -22 + -24 = 2 * -23; fraction 1 / -26.
Cost: repo-script WSL `lake build` green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`); DiagonalSumGeneral module 67s (68s at rung twenty-three; 10s at the a25 baseline — flat this round), full log build-2026-09-10-a48.log. Check 224 articles/197 fully proved (+5/+5 — the def node resolves by elaboration), 199 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 252 pages/201 nodes/597 code links (+5/+5/+5). Decision: negative ladder rung twenty-five ([-1]x27 at -1/27) follows the same factored pattern into factored-rungs-three/ (10 node slots remain) with preemptive 8192 depths on universal/existence (4096 prelims), witness split + witness-rules + one-line exacts retained; size outlook: universal 178/200 (growth ~5-6/rung, split horizon ~rung 29), crossings prelim 130/200 (~+5/rung, fine to ~rung 39), witness-rules 83/200 (~+7/rung, fine to ~rung 39), existence 151/200 (~+7/rung, fine to ~rung 32) — no further structural changes needed until the universal horizon. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twenty-five: ([-1]x27) inv carries -1/27 via factored preliminaries with witness-rules split

Proved ([-1]x27) inv carries -1/27: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a48 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twenty-seventh base crossing ⟨80,81,78,77,neg⟩ rejoins the prior rung's fresh arcs 78/77 per the add-gluing pattern (fresh arcs 80/81, +3 arc pattern, arc 79 unused), switching to ⟨81,78,77,80,pos⟩; matrix ⟨col80,col81,col0,col3⟩, fraction (col81-col80)/(col81-col3). Same neg_ne_zero pattern as rungs one to twenty-four for the negative denominator multiple.
Size design retained (quintet, as journaled): crossings prelim + universal + witness def + witness-rules + thin existence with one-line exacts. Raw spans post-edit (audit-style): prelim 137, universal 181, def 21, witness-rules 85, existence 155 — all under 200, namespace-aware. Wired as a49/a49a/a49b/a49c/a49d in factored-rungs-three/ from the start (container now 19 nodes; 5 slots remain before the next split). Blueprint depth flags honest (crossings prelim 4096, witness-rules 4096, universal/existence 8192; def none).
Depth setup (per standing orders): prelim/rules 4096, universal and existence 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all five elaborated with no new depth pressure once balanced. The witness def needs no override (plain if-else chain).
Generation notes: the rung-twenty-seven quintet was derived from the rung-twenty-six text by a mechanical transform script with asserted gates (9 expression sites now read run 28 / middles 225 / crossings 81, ALL-OK), carrying forward all prior corrections. Two transform defects, both caught before the green build: (1) the theorem-name gate asserted 2 residual OLD_N but counted 4 — OLD_N (26 negOnes) is a prefix-substring of NEW_RULES (first 26 of 27), so the genuine universal/existence renames now use a negative-lookahead replace (not followed by `_`); (2) the expression gate duplicated the 9 middle `.add` lines but missed the matching +1 first-line opens (run stayed 27, tails over-closed) — the first build failed with three parse errors at the new prelim-hc, universal-hc, and rules-region `.invert` tails (`unexpected token '.'`, `unexpected token ')'`, the rung-twenty balance class); a scoped fix inserted one `(` before `crossingTangle` on exactly the 9 new-block first-lines (runs 27→28, old block verified untouched at 27), then green. Structural re-reading confirmed junctions (rung-26 `rw [hdiv]` + blank into new prelim `set_option`), prelim 27-rule conclusion / 27-crossing list / 27 exact lines, universal r27 / e27 / hmRw / hnum / hden27 / c8081 / c813 / h27-triple / -1/27 with colon-free hc, def tail cells 80/81 = (-25,-24), witness-rules 27 conjuncts + 27 decide-pairs, existence 27-crossing hcs + ⟨-25,-24,2,3⟩ rfl + 27 one-line exacts + (-24) show-if/hdiv with -1/27, and the file tail (rw, blank, end). Witness extends the rung-twenty-six coloring by the forced pair (col80,col81)=(-25,-24), matrix ⟨-25,-24,2,3⟩. Hand-verified PD code: switched ⟨81,78,77,80,pos⟩ is (b,c,d,a) of base ⟨80,81,78,77,neg⟩ per the mirror-switch pattern; witness rule -24 = -24 and -23 + -25 = 2 * -24; fraction 1 / -27.
Cost: repo-script WSL `lake build` green on the second iteration (unbalanced build discarded), 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`); DiagonalSumGeneral module 74s (67s at rung twenty-four; 10s at the a25 baseline), full build 92.9s wall, full log build-2026-09-10-a49.log. Check 229 articles/202 fully proved (+5/+5 — the def node resolves by elaboration), 204 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 257 pages/206 nodes/602 code links (+5/+5/+5). Decision: negative ladder rung twenty-six ([-1]x28 at -1/28) follows the same factored pattern into factored-rungs-three/ (5 node slots remain) with preemptive 8192 depths on universal/existence (4096 prelims), witness split + witness-rules + one-line exacts retained; size outlook: universal 181/200 (growth ~3-6/rung, split horizon ~rung 29), crossings prelim 137/200 (~+7/rung, fine to ~rung 38), witness-rules 85/200 (~+2/rung this round, fine past rung 32), existence 155/200 (~+4/rung, fine to ~rung 32) — fold the first-line-open increment and OLD_N-lookahead gates into the transform from the start. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twenty-six: ([-1]x28) inv carries -1/28 via factored preliminaries with witness-rules split

Proved ([-1]x28) inv carries -1/28: universal claimed over a factored rule-equation preliminary plus direct witness, mirroring a49 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twenty-eighth base crossing <83,84,81,80,neg> rejoins the prior rung's fresh arcs 81/80 per the add-gluing pattern (fresh arcs 83/84, +3 arc pattern, arc 82 unused), switching to <84,81,80,83,pos>; matrix <col83,col84,col0,col3>, fraction (col84-col83)/(col84-col3). Same neg_ne_zero pattern as rungs one to twenty-five for the negative denominator multiple.
Depth setup (per standing orders): prelim/rules 4096, universal and existence 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all five elaborated first try at depth 28, no new depth pressure observed. The witness def is a plain if-else chain and carries no override. Blueprint depth flags honest (crossings prelim 4096, witness-rules 4096, universal/existence 8192; def none).
Size design retained (quintet): crossings prelim + universal + witness def + witness-rules + thin existence with one-line exacts. True spans post-edit (audit-style, decl-line to next-decl-line): prelim 141, universal 190, def 17, witness-rules 90, existence 162 — all under 200, namespace-aware. Wired as a50/a50a/a50b/a50c/a50d in factored-rungs-three/ from the start (container now 24 nodes: AT the 24-article limit — the next rung must create the next sub-chapter per the a36/a40/a46 pattern). Size outlook: universal 190/200 (growth ~8-9/rung, split horizon rung 29 — a universal-side split or tighter linarith packaging will be needed next round); crossings prelim 141/200 (~+4/rung, fine to ~rung 40), witness-rules 90/200 (~+5/rung, fine past rung 32), existence 162/200 (~+7/rung, fine to ~rung 33).
Generation notes: the rung-twenty-eight quintet was derived from the rung-twenty-seven text by a mechanical transform script with asserted gates (9 expression sites now read run 29 / middles 234 / crossings 84, ALL-OK), carrying forward all prior corrections with the OLD_N-lookahead and first-line-open gates folded in from the start. New gate calibrations this round, all caught pre-write: the 27th negOne has no trailing underscore (universal/existence decl names end `negOne (col` / `negOne :` — the name patterns must be built as negOne_*26 + negOne, not negOne_*27); the hval statement line carries both diffs (numerator-diff count is 6 not 5: hnum/hnumR/hval-stmt/hfrac-type/divInt/show-RHS; denominator-diff count is 8 not 6: hden-decl/hden/hdenR/hval-stmt/hfrac-type/if-cond/divInt/show-RHS); the multiplier `27 * (col 2 - col 1)` is 4 sites not 3 (the hden-decl line carries one too); the divInt args repeat in show and hdiv (count 2); stale-differential gating must target old tail forms (col-0 conclusion tail, `] := by` list tail, `(by decide)>` exact tail) since arcs 81 legitimately persist in rules 27-28; retention needs terminator anchoring (`^theorem name$`, `have hrules := name`, `name (col`, `name :`) because OLD names are prefix-substrings of NEW names; the tails gate must match `.invert[).]` (the existence decl line contains `.invert_add_`); the hc-colon gate checks tail lines (prelim tail ends `IsColored col) :`, universal tail ends colon-free followed by the hm binder).
Process incidents (recovered, no mathematics lost): (1) the transform's multi-line inserts used bare-LF joins, so the first spliced write mixed 9 bare-LF lines into the all-CRLF file — caught by the post-write byte gate (pre-write gate had passed); recovered by normalizing bare LFs to CRLF in place (file was all-CRLF before, so this exactly restores convention; 9 lines normalized, matching the 9 embedded inserts) and re-verifying read-only plus full structural re-read; the script now flattens inserts to true lines before gating (which also corrected span projections: universal 189 -> true 190). (2) the blueprint script never extended the frontmatter negOne chains (27, not 28) — caught on re-read; fixed with terminator-anchored replaces (old-chain lookahead `(?![_])` since the 27-chain is a suffix of the 28-chain), all three theorem nodes verified at 28 negOnes. Structural re-reading confirmed the full new block (601 lines: prelim 28-rule conclusion / 28-crossing list / 28 exact lines, universal r28 / e28 / hmRw / hnum / hden28 / c8384 / c843 / h28-triple / -1/28 with colon-free hc tail, def tail cells 83/84 = (-26,-25), witness-rules 28 conjuncts + 28 decide-pairs, existence 28-crossing hcs + <-26,-25,2,3> rfl + 28 one-line exacts + (-25) show-if/hdiv with -1/28, and the file tail (rw, blank, end)). Witness extends the rung-twenty-seven coloring by the forced pair (col83,col84)=(-26,-25), matrix <-26,-25,2,3>. Hand-verified PD code: switched <84,81,80,83,pos> is (b,c,d,a) of base <83,84,81,80,neg> per the mirror-switch pattern; witness rule -25 = -25 and -24 + -26 = 2 * -25; fraction 1 / -28.
Cost: repo-script WSL lake build green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 hok1); DiagonalSumGeneral module 75s (74s at rung twenty-five; 10s at the a25 baseline), full build 96.5s wall, full log build-2026-09-10-a50.log. Check 234 articles/207 fully proved (+5/+5), 209 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 262 pages/211 nodes/607 code links (+5/+5/+5). Decision: negative ladder rung twenty-seven ([-1]x29 at -1/29) CANNOT go into factored-rungs-three/ as a quintet (24 + 5 = 29 > 24 — create factored-rungs-four per the a36/a40/a46 pattern first), needs a universal-side size strategy (190/200 now; flat projects ~198+), preemptive 8192 depths on universal/existence (4096 prelims), witness split + witness-rules + one-line exacts retained; fold this round's calibrations (no-trailing-underscore names, 6/8 diff counts, 4-site multiplier, 2-site divInt, tail-form stale gates, terminator-anchored retention, `.invert[).]` tails, insert flattening, frontmatter chain extension) into the transform from the start. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twenty-seven: ([-1]x29) inv carries -1/29 via factored preliminaries with fraction-value split

Proved ([-1]x29) inv carries -1/29: universal claimed over TWO factored preliminaries (crossings rules plus a NEW fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a50 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): twenty-ninth base crossing <86,87,84,83,neg> rejoins the prior rung's fresh arcs 84/83 per the add-gluing pattern (fresh arcs 86/87, +3 arc pattern, arc 85 unused), switching to <87,84,83,86,pos>; matrix <col87,col86,col0,col3>, fraction (col87-col86)/(col87-col3). Same neg_ne_zero pattern as rungs one to twenty-six for the negative denominator multiple.
Container first (honest, per the sub-chapter rule): factored-rungs-three/ was FULL (24/24), so neg-ladder/factored-rungs-four/ was created BEFORE wiring (README + parent-ladder link; nothing moved — new rungs go in the new container, now 7 nodes: index + sextet).
Size strategy executed (as mandated): the universal stood at 190/200 and flat-projected ~198+, so its linarith tail (obtains + hnum/hden/hd/hnumR/hdenR/hval) was factored into its own preliminary `coloring_fraction_invert_add_(29xnegOne)_fraction_value`, stating (col 87 - col 3 ≠ 0) ∧ (value = -1/29) over the 29 rule equations plus the non-mono hypothesis (same witness-rules precedent); the thin universal keeps only the hrules/hmRw obtains, the hfrac block, and the final rw. Raw spans post-edit (audit-style, measured from disk): prelim 145, value 97, universal 140, def 18, witness-rules 93, existence 166 — all under 200, namespace-aware. Wired as a51/a51a/a51b/a51c/a51d/a51e (sextet: the a-suffix now holds the fraction-value lemma and the universal moves to b — convention shift recorded here). Blueprint depth flags honest (crossings prelim 4096, value 8192 since it holds the exact linarith block that forced the 8192 bump at rung twenty-two, universal/existence 8192; def none).
Depth setup (per standing orders): prelim/rules 4096, value/universal/existence 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all six elaborated with no new depth pressure (green first build post-review at depth 29). The witness def is a plain if-else chain and carries no override.
Generation notes: the rung-twenty-nine sextet was derived from the rung-twenty-eight text by a mechanical transform script with asserted gates (9 expression sites now read run 30 / middles 243 / crossings 87, ALL-OK), carrying forward all prior calibrations. New gate calibrations this round: the witness-name count is 146 not 116 (28 rules × 5 — the `2 * W` factor carries a third name — plus 2 decls, hw/hM uses, rules-docstring, existence refine); hden28 is 3 sites not 2 (decl + rw-use + exact_mod_cast-use); the thin universal negOne is 87 not 58 (decl + prelim-use + value-use); the Lean existence carries no twenty-eight-unit prose / 1/-28 / w-range (blueprint-only strings — counts 0/skip, fractions 4 not 5); prelim conjunct lines mix indents (first 28 at 4-space, last bare — match `^ *`); multi-line replaces must go through a block-level \n-join (per-line replace embeds newlines — the a50 lesson re-met); depends-link filenames need explicit remap BEFORE any global number-bump (a50b→a51c, a50c→a51d) or the bump corrupts them; the prelim blueprint chain needs its own +1-link gate (caught post-write still at 28, fixed); a51d units are 4 not 3 (title + coloring + switched + one-line). Re-reading caught what the gates missed (the structural backstop, four misses): the value obtain dropped r28 (`r27, r29⟩` — anchor on the fuller `r27, r28⟩` form going forward); the c8687/c873 STATEMENTS still read col 83/84 (names were renamed but the equation forms were never in any gate — stale-equality residual gates added); the rules docstring still said rung-twenty-eight (only the `-unit` form was gated — residual `twenty-eight`/`TwentyEight` gates added); two missing blank separators at the new value→universal and universal→def joints — all fixed by an asserted patch (file all-CRLF re-verified, residuals zero). Hand-verified PD code: switched <87,84,83,86,pos> is (b,c,d,a) of base <86,87,84,83,neg> per the mirror-switch pattern; witness rule -26 = -26 and -25 + -27 = 2 * -26; fraction 1 / -29. Witness extends the rung-twenty-six (x28) coloring by the forced pair (col86,col87)=(-27,-26), matrix <-27,-26,2,3>. Note: a51c was templated from a49b (correct prior-unit convention, "extending the twenty-eight-unit coloring"); a50b's off-by-one prose ("extending the twenty-eight-unit coloring" for the x28 witness) is left untouched — prior artifacts stay as-is.
Cost: repo-script WSL `lake build` green first try post-review, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`); DiagonalSumGeneral module 81s (75s at rung twenty-six; 10s at the a25 baseline), full log build-2026-09-10-a51.log. Check 241 articles/213 fully proved (+7/+6 — the extra article is the new sub-chapter index), 215 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 270 pages/217 nodes/613 code links (+8/+6/+6 — pages include the new index plus per-node dependency views).
Decision: negative ladder rung twenty-eight ([-1]x30 at -1/30) follows the same sextet pattern into factored-rungs-four/ (17 node slots remain) with preemptive 8192 depths on value/universal/existence (4096 prelims/rules), witness split + witness-rules + one-line exacts retained; size outlook: universal 140/200 (~+5/rung, fine past rung 35), value 97/200 (~+2/rung, fine past rung 40), crossings prelim 145/200 (~+5/rung, fine to ~rung 39), witness-rules 93/200 (~+5/rung, fine past rung 32), existence 166/200 (~+4/rung, fine to ~rung 33) — no further structural changes needed until the existence horizon ~rung 33; fold this round's calibrations (146/3-site/87-counts, mixed-indent conjuncts, block replaces, depends-first ordering, chain gates, stale-equality residuals) into the transform from the start. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twenty-eight: ([-1]x30) inv carries -1/30 via factored preliminaries with fraction-value split

Proved ([-1]x30) inv carries -1/30: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a51 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirtieth base crossing <89,90,87,86,neg> rejoins the prior rung's fresh arcs 87/86 per the add-gluing pattern (fresh arcs 89/90, +3 arc pattern, arc 88 unused), switching to <90,87,86,89,pos>; matrix <col90,col89,col0,col3>, fraction (col90-col89)/(col90-col3). Same neg_ne_zero pattern as rungs one to twenty-seven for the negative denominator multiple.
Size design retained (sextet): crossings prelim + fraction-value + thin universal + witness def + witness-rules + thin existence with one-line exacts. Raw spans post-edit (audit-style, measured from disk): prelim 150, value 99, universal 145, def 17, witness-rules 96, existence 172 — all under 200, namespace-aware. Wired as a52/a52a/a52b/a52c/a52d/a52e in factored-rungs-four/ from the start (container now 13 nodes: index + 2 sextets; 11 slots remain before the next split). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal 8192, def none, witness-rules 4096, existence 8192).
Depth setup (per standing orders): prelim/rules 4096, value/universal/existence 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all six elaborated first try at depth 30, no new depth pressure observed. The witness def is a plain if-else chain and carries no override.
Generation notes: the rung-thirty sextet was derived from the rung-twenty-nine text by a mechanical transform script with asserted gates (9 expression sites now read run 31 / middles +9 / crossings 90, ALL-OK), carrying forward all prior calibrations. New gate calibrations this round, all caught pre-write: spaced `-1 / 29` is 2 sites not 1 (hval type + hval statement); `h29ne`/`h29n` are 2 lines each not 1 (decl + use — the use was forgotten); the show-`divInt` witness form is 2 sites not 1 (show-RHS + hdiv decl); c-lines are 2 lines each not 3 (decl + hmRw-use; the 3-count belonged to hden); append-type inserts must NOT assert old-substring residuals (the old line persists by design — e30 obtain, exact-w30, rules-proof tail now gate the NEW fragment instead); the 29-link chain residual gate is substring-bogus on 30-chains (links 2-30 match — gate total `[-1]` links == 90 plus 30-link lines == 3 instead, the a45 Q22/Q23 class re-met). Structural re-reading caught one REAL defect the gates missed: three literal backslash-n two-character sequences embedded in the new block (the late e30/exact-w30/rules-proof patches typed `\\n` in edit parameters, landing as text rather than newlines — the file still passed the no-bare-LF gate since backslash is not LF); fixed by a targeted 3-site patch (file all-CRLF re-verified, zero literal-`\n` post-fix, spans re-measured from disk). Witness extends the rung-twenty-nine coloring by the forced pair (col89,col90)=(-28,-27), matrix <-28,-27,2,3>. Hand-verified PD code: switched <90,87,86,89,pos> is (b,c,d,a) of base <89,90,87,86,neg> per the mirror-switch pattern; witness rule -27 = -27 and -26 + -28 = 2 * -27; fraction 1 / -30.
Cost: repo-script WSL `lake build` green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`; one replayed-module Coloring.lean simp warning, pre-existing); DiagonalSumGeneral module 88s (81s at rung twenty-seven; 10s at the a25 baseline), full log build-2026-09-10-a52.log. Check 247 articles/219 fully proved (+6/+6), 221 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 276 pages/223 nodes/619 code links (+6/+6/+6).
Decision: negative ladder rung twenty-nine ([-1]x31 at -1/31) follows the same sextet pattern into factored-rungs-four/ (11 slots remain) with preemptive 8192 depths on value/universal/existence (4096 prelims/rules), witness split + witness-rules + one-line exacts retained; size outlook: universal 145/200 (~+5/rung, fine past rung 35), value 99/200 (~+2/rung, fine past rung 40), crossings prelim 150/200 (~+5/rung, fine to ~rung 38), witness-rules 96/200 (~+5/rung, fine past rung 32), existence 172/200 (~+6/rung, horizon ~rung 32-33) — fold this round's calibrations (dual-fraction/dual-h29n/dual-divInt counts, c-line pairs, append-gating, link-total chains, single-backslash newlines in late patches) into the transform from the start. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung twenty-nine: ([-1]x31) inv carries -1/31 via factored preliminaries with fraction-value split

Proved ([-1]x31) inv carries -1/31: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a52 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirty-first base crossing <92,93,90,89,neg> rejoins the prior rung's fresh arcs 90/89 per the add-gluing pattern (fresh arcs 92/93, +3 arc pattern, arc 91 unused), switching to <93,90,89,92,pos>; matrix <col93,col92,col0,col3>, fraction (col93-col92)/(col93-col3). Same neg_ne_zero pattern as rungs one to twenty-eight for the negative denominator multiple.
Depth setup (per standing orders): prelim/rules 4096, value/universal/existence 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all six elaborated first try at depth 31, no new depth pressure observed. The witness def is a plain if-else chain and carries no override. Blueprint depth flags honest (crossings prelim 4096, value 8192, universal 8192, def none, witness-rules 4096, existence 8192).
Size design retained (sextet): crossings prelim + fraction-value + thin universal + witness def + witness-rules + thin existence with one-line exacts. Raw spans post-edit (audit-style, measured from disk): prelim 155, value 101, universal 149, def 17, witness-rules 99, existence 176 — all under 200, namespace-aware. Wired as a53/a53a/a53b/a53c/a53d/a53e in factored-rungs-four/ from the start (container now 19 nodes: index + 3 sextets; 5 slots remain before the next split).
Generation notes: the rung-thirty-one sextet was derived from the rung-thirty text by a mechanical transform script with asserted gates (9 expression sites now read run 32 / middles 252+9=261 / crossings 93, ALL-OK), carrying forward all prior calibrations. Two script defects, both caught pre-write by the gates themselves: (1) the def-rename residual gate is substring-bogus (NegThirty is a prefix of NegThirtyOne — the a45 Q22/Q23 class re-met; replaced with a lookahead-anchored gate); (2) three l.replace inserts carried doubled backslash-n escapes (typed after the JSON-escaped Read display — the a52 incident class re-met from the other side); the new literal-backslash-n gate fired with n=3 and a byte-level PowerShell replace fixed exactly those 3 lines, verified zero post-fix. New gate calibration: the ordinal sub adds a bare count (thirtieth contains no thirty substring — thirty-first does), so prelim bare-thirty expects 5 not 4. Structural re-reading confirmed the full new block (697 lines: prelim 31-rule conclusion / 31-crossing list / 31 exact lines, universal r31 / e31 / hmRw / hnum / hden31 / c9293 / c933 / h31-triple / -1/31 with colon-free hc, def tail cells 92/93 = (-29,-28), witness-rules 31 conjuncts + 31 decide-pairs, existence 31-crossing hcs + <-29,-28,2,3> rfl + 31 one-line exacts + (-28) show-if/hdiv with -1/31, and the file tail (rw, blank, end)). Witness extends the rung-thirty coloring by the forced pair (col92,col93)=(-29,-28), matrix <-29,-28,2,3>. Hand-verified PD code: switched <93,90,89,92,pos> is (b,c,d,a) of base <92,93,90,89,neg> per the mirror-switch pattern; witness rule -28 = -28 and -27 + -29 = 2 * -28; fraction 1 / -31.
Blueprint notes: bp53.py derived the sextet from a52* with asserted gates (full Depends-line swaps first, then bare-only thirty subs via lookahead). Re-reading caught three misses the gates did not cover: the ordinal read thirty-one-first (fixed to thirty-first), the witness node said extending the thirty-one-unit coloring (prior-rung reference — fixed to thirty-unit), and the rules size note still read ~172 (fixed to ~176). Frontmatter verified per node (single lean lines with 31-link chains, proof flags present, Depends on a53*).
Cost: repo-script WSL lake build green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 hok1); DiagonalSumGeneral module 95s (88s at rung twenty-eight; 10s at the a25 baseline), full log build-2026-09-10-a53.log. Check 253 articles/225 fully proved (+6/+6), 227 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 282 pages/229 nodes/625 code links (+6/+6/+6).
Decision: negative ladder rung thirty ([-1]x32 at -1/32) CANNOT go into factored-rungs-four/ as a sextet (19 + 6 = 25 > 24 — create factored-rungs-five per the a36/a40/a46/a51 pattern first), then follow the same sextet pattern with preemptive 8192 depths on value/universal/existence (4096 prelims/rules), witness split + witness-rules + one-line exacts retained; size outlook: existence 176/200 (~+5/rung, horizon ~rung 33-34), universal 149/200 (fine past rung 35), value 101/200 (fine past rung 40), crossings prelim 155/200 (fine to ~rung 37), witness-rules 99/200 (fine past rung 32). The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung thirty: ([-1]x32) inv carries -1/32 via factored preliminaries with fraction-value split

Proved ([-1]x32) inv carries -1/32: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a53 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirty-second base crossing <95,96,93,92,neg> rejoins the prior rung's fresh arcs 93/92 per the add-gluing pattern (fresh arcs 95/96, +3 arc pattern, arc 94 unused), switching to <96,93,92,95,pos>; matrix <col96,col95,col0,col3>, fraction (col96-col95)/(col96-col3). Same neg_ne_zero pattern as rungs one to twenty-nine for the negative denominator multiple.
Container first (honest, per the sub-chapter rule): factored-rungs-four/ was at 19 nodes (index + 3 sextets) and a sextet cannot fit (19 + 6 = 25 > 24), so neg-ladder/factored-rungs-five/ was created BEFORE wiring (README + parent-ladder README header and Nodes links; nothing moved — new rungs go in the new container, now 7 nodes: index + sextet); the container README states the 19+6 rationale.
Depth setup (per standing orders): prelim/rules 4096, value/universal/existence 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all six elaborated first try at depth 32, no new depth pressure observed. The witness def is a plain if-else chain and carries no override. Blueprint depth flags honest (crossings prelim 4096, value 8192, universal 8192, def none, witness-rules 4096, existence 8192).
Size design retained (sextet): crossings prelim + fraction-value + thin universal + witness def + witness-rules + thin existence with one-line exacts. Raw spans post-edit (audit-style, set_option/def to next boundary, measured from disk): prelim 160, value 103, universal 153, def 17, witness-rules 102, existence 181 — all under 200, namespace-aware. Wired as a54/a54a/a54b/a54c/a54d/a54e in factored-rungs-five/ from the start. Blueprint size notes updated honestly (universal ~153, existence ~181).
Generation notes: the rung-thirty-two sextet was derived from the rung-thirty-one text by a mechanical transform script with asserted gates (9 expression sites now read run 33 / middles 270 = 90 six-space + 180 eight-space / crossings 96 / _negOne 192 = 6 name lines x32 / [-1] 96 = 3 doc lines x32, ALL-OK), carrying forward all prior calibrations. Script-gate defects, all caught pre-write by the gates themselves: (1) substring counting conflated the two middle indent forms (6-space lines are substrings of 8-space lines — gates now use exact-line equality: 87/174 old, 90/180 new); (2) str-literal hex escapes for the conjunction do not survive .encode() (gates now use literal UTF-8 chars; bytes-literal escapes were already correct); (3) multi-line inserts must flatten to true lines before gating (the a50 lesson, applied — zero literal-backslash-n post); (4) the negOne_negOne_negOne_rules residual is substring-bogus on 32-chains (replaced with an exact chain-length gate: 6 lines at 32 _negOne). Count calibrations: ThirtyTwo 166 = 161 renamed + 5 in the two new 96-rule lines (T32_96 = 2, T32_95 = 1); universal thirty-one 6 (Depends titles+filenames first, then bare); rules thirty-one 7; existence -1/31 x2, thirty-one x8, sharpCol x1 (prose only — its lean is the long chain); presw crossing x2. Representation facts re-met: the w1 … w32 range prose lives in the existence node only (the rules node carries none); the existence node carries no literal [-1] chain (witness-named). Structural re-reading confirmed the full new block (717 lines: prelim 32-rule conclusion / 32-crossing list / 32 exact lines, universal r32 / e32 / hmRw / hnum / hden32 / c9596 / c963 / h32-triple / -1/32 with colon-free hc, def tail cells 95/96 = (-30,-29), witness-rules 32 conjuncts + 32 decide-pairs, existence 32-crossing hcs + <-30,-29,2,3> rfl + 32 one-line exacts + (-29) show-if/hdiv with -1/32, and the file tail (rw, blank, end)). Witness extends the rung-thirty-one coloring by the forced pair (col95,col96)=(-30,-29), matrix <-30,-29,2,3>. Hand-verified PD code: switched <96,93,92,95,pos> is (b,c,d,a) of base <95,96,93,92,neg> per the mirror-switch pattern; witness rule -29 = -29 and -28 + -30 = 2 * -29; fraction 1 / -32.
Blueprint notes: bp54.py derived the sextet from a53* with asserted gates (full Depends-line swaps first, then bare-only thirty-two subs; prior-rung reference fixed to thirty-one-unit, ordinal to thirty-second). Re-reading confirmed all six nodes with no misses. Frontmatter verified per node (single lean lines with 32-link chains on the four theorem nodes, def-names on witness/rules, proof flags present, Depends on a54*). Byte gates: blueprint nodes LF (repo convention, matching a53*), Lean file all-CRLF, zero literal-backslash-n.
Cost: repo-script WSL lake build green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 hok1; Coloring.lean simp warnings are replayed-module pre-existing); DiagonalSumGeneral module 100s (95s at rung twenty-nine; 10s at the a25 baseline), full log build-2026-09-10-a54.log. Check 260 articles/231 fully proved (+7/+6 — the extra article is the new sub-chapter index), 233 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 290 pages/235 nodes/631 code links (+8/+6/+6 — pages include the new index plus per-node dependency views).
Decision: negative ladder rung thirty-one ([-1]x33 at -1/33) follows the same sextet pattern into factored-rungs-five/ (17 node slots remain) with preemptive 8192 depths on value/universal/existence (4096 prelims/rules), witness split + witness-rules + one-line exacts retained; size outlook: existence 181/200 (~+5/rung, split horizon ~rung 33-34), universal 153/200 (fine past rung 35), value 103/200 (fine past rung 40), crossings prelim 160/200 (fine to ~rung 37), witness-rules 102/200 (fine past rung 32) — fold this round's calibrations (exact-line middle counts, literal-char conjunctions, chain-length gate, 166/6/7/2/8/1 counts, w-range/existence-chain representation facts) into the transform from the start. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung thirty-one: ([-1]x33) inv carries -1/33 via factored preliminaries with fraction-value split

Proved ([-1]x33) inv carries -1/33: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a54 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirty-third base crossing ⟨98,99,96,95,neg⟩ rejoins the prior rung's fresh arcs 96/95 per the add-gluing pattern (fresh arcs 98/99, +3 arc pattern, arc 97 unused), switching to ⟨99,96,95,98,pos⟩; matrix ⟨col99,col98,col0,col3⟩, fraction (col99-col98)/(col99-col3). Same neg_ne_zero pattern as rungs one–thirty for the negative denominator multiple.
Depth setup (per standing orders): prelim/rules 4096, value/universal/existence 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all six elaborated first try at depth 33, no new depth pressure observed. The witness def is a plain if-else chain and carries no override. Blueprint depth flags honest (crossings prelim 4096, value 8192, universal 8192, def none, witness-rules 4096, existence 8192).
Size design retained (sextet): crossings prelim + fraction-value + thin universal + witness def + witness-rules + thin existence with one-line exacts. Raw spans post-edit (audit-style, set_option/def to next decl start, measured from disk): prelim 165, value 105, universal 157, def 17, witness-rules 105, existence 186 — all under 200, namespace-aware. Wired as a55/a55a/a55b/a55c/a55d/a55e in factored-rungs-five/ from the start (container now 13 nodes: index + 2 sextets; 11 slots remain before the next split). Blueprint size notes updated honestly (value ~157, rules/existence ~186).
Generation notes: the rung-thirty-three sextet was derived from the rung-thirty-two text by a mechanical transform script with asserted gates (9 expression sites now read run 34 / middles 279 = 93 six-space + 186 eight-space / crossings 99 / _negOne 198 = 6 name lines x33 / [-1] 99 = 3 doc lines x33, ALL-OK), carrying forward all prior calibrations. Two script defects, both caught BEFORE any write by the gates themselves: (1) the splice sliced prefix-before-old-block instead of prefix-including-old-block (replace, not append — the a43 incident class; the new retention gate asserting prior-rung names in the final text fired); (2) the span gate took bounds[-6:] over set_option+theorem/def boundaries (misaligned segments printing 152/17/4/101/2/184 — the gate now pairs the 6 set_option/def decl starts within the new region). Post-write, one cosmetic defect: no blank separator at the old-tail→new-prelim junction (the a51 class; one-line fix, spans unaffected since they start at set_option). Structural re-reading confirmed the full new block (735 lines: prelim 33-rule conclusion / 33-crossing list / 33 exact lines, universal r33 / e33 / hmRw / hnum / hden33 / c9899 / c993 / h33-triple / -1/33 with colon-free hc, def tail cells 98/99 = (-31,-30), witness-rules 33 conjuncts + 33 decide-pairs, existence 33-crossing hcs + ⟨-31,-30,2,3⟩ rfl + 33 one-line exacts + (-30) show-if/hdiv with -1/33, and the file tail (rw, blank, end)). Witness extends the rung-thirty-two coloring by the forced pair (col98,col99)=(-31,-30), matrix ⟨-31,-30,2,3⟩. Hand-verified PD code: switched ⟨99,96,95,98,pos⟩ is (b,c,d,a) of base ⟨98,99,96,95,neg⟩ per the mirror-switch pattern; witness rule -30 = -30 and -29 + -31 = 2 * -30; fraction 1 / -33.
Blueprint notes: bp55.py derived the sextet from a54* in the same container with asserted gates (full Depends-line swaps first, then bare-only thirty-three subs; prior-rung reference fixed to thirty-two-unit, ordinal to thirty-third; size notes to measured ~157/~186). Re-reading confirmed all six nodes with no misses. Frontmatter verified per node (single lean lines with 33-link chains on the four theorem nodes, def-names on witness/rules, proof flags present, Depends on a55*). Byte gates: blueprint nodes LF (repo convention), Lean file all-CRLF, zero literal-backslash-n.
Cost: repo-script WSL `lake build` green first try (junction fix was pre-build), 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`; ColorFractionUnique simp warnings are replayed-module pre-existing); DiagonalSumGeneral module 105s (100s at rung thirty; 10s at the a25 baseline), full log build-2026-09-10-a55.log. Check 266 articles/237 fully proved (+6/+6), 239 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 296 pages/241 nodes/637 code links (+6/+6/+6).
Decision: negative ladder rung thirty-two ([-1]x34 at -1/34) follows the same sextet pattern into factored-rungs-five/ (11 slots remain) with preemptive 8192 depths on value/universal/existence (4096 prelims/rules), witness split + witness-rules + one-line exacts retained; size outlook: existence 186/200 (~+5/rung, split horizon ~rung 34-35 — plan the next size strategy now: split the existence fraction computation from the one-line exacts, or factor the hcs/rfl blocks), universal 157/200 (fine past rung 35), value 105/200 (fine past rung 40), crossings prelim 165/200 (fine to ~rung 36), witness-rules 105/200 (fine past rung 35) — fold this round's calibrations (append-splice retention, decl-start span pairing, junction-blank check) into the transform from the start. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung thirty-two: ([-1]x34) inv carries -1/34 via factored preliminaries with fraction-value split

Proved ([-1]x34) inv carries -1/34: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a55 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirty-fourth base crossing <101,102,99,98,neg> rejoins the prior rung's fresh arcs 99/98 per the add-gluing pattern (fresh arcs 101/102, +3 arc pattern, arc 100 unused), switching to <102,99,98,101,pos>; matrix <col102,col101,col0,col3>, fraction (col102-col101)/(col102-col3). Same neg_ne_zero pattern as rungs one to thirty-one for the negative denominator multiple.
Depth setup (per standing orders): prelim/rules 4096, value/universal/existence 8192 from the start (placed before the docstrings) — the rung-twenty-two thresholds carried forward preemptively; all six elaborated first try at depth 34, no new depth pressure observed. The witness def is a plain if-else chain and carries no override. Blueprint depth flags honest (crossings prelim 4096, value 8192, universal 8192, def none, witness-rules 4096, existence 8192).
Size design retained (sextet, no structural change): crossings prelim + fraction-value + thin universal + witness def + witness-rules + thin existence with one-line exacts. True spans post-edit (audit-style, decl-start to next-decl-line, measured from disk): prelim 170, value 107, universal 161, def 17, witness-rules 108, existence 191 — all under 200, namespace-aware. Wired as a56/a56a/a56b/a56c/a56d/a56e in factored-rungs-five/ from the start (container now 19 nodes: index + 3 sextets; 5 slots remain before the next split).
Generation notes: the rung-thirty-four sextet was derived from the rung-thirty-three text by a mechanical transform script with asserted gates (9 expression sites now read run 35 / middles 288 = 96 six-space + 192 eight-space / crossings 102 / _negOne 34 x6 name lines / [-1] 102 = 3 doc lines x34, ALL-OK), carrying forward all prior calibrations. New gate calibrations this round, all caught pre-write: crossings must count occurrences not lines (85 lines carry 99 crossings — multi-crossing list lines); -33d is 2 sites not 1 (value + universal docstrings); the new rules conjunct adds 5 legitimate sharpCol occurrences (176 not 171 post-insert); the tails content gate must normalize witness names (hM line is filter-excluded, 9==9 identical); blueprint prose [-1] chains ARE extended per rung (a54 32, a55 33 — the a56 prelim/universal prose needed a +1-link fix post-write, caught by cross-rung chain audit); the rules-name retention expects 2 (decl + hw-use, not 1); the existence ~186 note is 1 site not 2 (the parenthetical lives in the rules node). Two process defects, both caught before the green build: (1) the first splice DROPPED the old block (replace not append — the a43 incident class re-met from the other side; the retention gate asserting prior-rung names in the FINAL text fired); fixed to prefix + old-body + new + end with terminator-anchored retention gates. (2) an inline-shell chain patch mangled brackets (`[-1+[-1])` — new standing lesson: chain patches go through script files, never inline). Structural re-reading confirmed the full new block (755 lines: prelim 34-rule conclusion / 34-crossing list / 34 exact lines, universal r34 / e34 / hmRw / hnum / hden34 / c101102 / c1023 / h34-triple / -1/34 with colon-free hc, def tail cells 101/102 = (-32,-31), witness-rules 34 conjuncts + 34 decide-pairs, existence 34-crossing hcs + <-32,-31,2,3> rfl + 34 one-line exacts + (-31) show-if/hdiv with -1/34, and the file tail (rw, blank, end)). Witness extends the rung-thirty-three coloring by the forced pair (col101,col102)=(-32,-31), matrix <-32,-31,2,3>. Hand-verified PD code: switched <102,99,98,101,pos> is (b,c,d,a) of base <101,102,99,98,neg> per the mirror-switch pattern; witness rule -31 = -31 and -30 + -32 = 2 * -31; fraction 1 / -34.
RUNG-THIRTY-SIX SIZE DESIGN (recorded, NOT implemented): existence span is now 191/200 and grows ~5/rung, so rung thirty-five projects ~196 (fits) but rung thirty-six ([-1]x36) projects ~201 and exceeds the limit in its current shape. Design adopted for that round: factor the existence TAIL into its own preliminary — a new theorem `sharpColAddNegThirtySix_witness_value` over the witness coloring stating the matrix equation, NotMono, and fraction value (proved by the hM rfl, the NotMono decide, and the show-if/hdiv normalization, ~20 lines fixed cost), claimed in its own node beside witness-rules; the existence node then keeps only the hcs-list decide, the w-obtains, the one-line exacts, and the final assembly over both preliminaries (~160 lines and growing only ~3/rung thereafter). Rationale: mirrors the rung-twenty-nine universal-to-value split precedent (fixed-cost tail out, growth-bearing body stays); the crossings prelim (170/200, ~+5/rung, fine to ~rung 36) and universal (161/200, ~+4/rung, fine to ~rung 38) need no change yet. Rejected alternative: splitting the one-line exacts across two nodes (breaks the single-obtain single-use pattern and complicates the rcases spine for no span gain on the tail). Implement it during the rung-thirty-six round only if existence projects past 200 then; rung thirty-five keeps the sextet unchanged. The negative-family induction question remains open.
Cost: repo-script WSL `lake build` green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok1`); DiagonalSumGeneral module 111s (105s at rung thirty-one; 10s at the a25 baseline), full log build-2026-09-10-a56.log. Check 272 articles/243 fully proved (+6/+6), 245 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 302 pages/247 nodes/643 code links (+6/+6/+6).
Decision: negative ladder rung thirty-three ([-1]x35 at -1/35) CANNOT go into factored-rungs-five/ as a sextet (19 + 6 = 25 > 24 — create factored-rungs-six per the a36/a40/a46/a51/a54 pattern first), then follow the same sextet pattern with preemptive 8192 depths on value/universal/existence (4096 prelims/rules), witness split + witness-rules + one-line exacts retained; rung thirty-six then implements the witness-value split above in the new container (which will sit at 11 nodes after two sextets); fold this round's calibrations (occurrence-based crossings, dual -33d, +5 rules-name inserts, normalized tails, prose-chain extension, decl+use retention counts, script-file-only chain patches) into the transform from the start. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung thirty-three: ([-1]x35) inv carries -1/35 via factored preliminaries with fraction-value split

Proved ([-1]x35) inv carries -1/35: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a56 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirty-fifth base crossing <104,105,102,101,neg> rejoins the prior rung's fresh arcs 102/101 per the add-gluing pattern (fresh arcs 104/105, +3 arc pattern, arc 103 unused), switching to <105,102,101,104,pos>; matrix <col105,col104,col0,col3>, fraction (col105-col104)/(col105-col3). Same neg_ne_zero pattern as rungs one to thirty-two for the negative denominator multiple.
Container first (honest, per the sub-chapter rule): factored-rungs-five/ was at 19 nodes (index + 3 sextets) and a sextet cannot fit (19 + 6 = 25 > 24), so neg-ladder/factored-rungs-six/ was created BEFORE wiring (README + parent-ladder README header and Nodes links; nothing moved — new rungs go in the new container, now 7 nodes: index + sextet); the container README states the 19+6 rationale.
Depth wrinkle (new this round): `set_option maxRecDepth 8192` — sufficient since rung twenty-two — no longer suffices at depth 35: the universal's `hm`-typing step (line 16603) and the existence `rfl` matrix computation (line 16921) both hit maximum-recursion-depth errors, while the preliminary `decide`, the witness-rules, and the value linarith tail still elaborate at their old levels. Bumped exactly the two failing declarations to `set_option maxRecDepth 16384` (placed before the docstrings, via an anchored script patch); prelim/rules stay at 4096, value stays at 8192. Two build iterations: first failed only on the two depth errors (caught by Lean, fixed, re-read); then green, 1005 jobs, zero sorry/admit/axiom, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok1`). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence 16384; def none; witness-rules 4096).
Size design retained (sextet, no structural change): crossings prelim + fraction-value + thin universal + witness def + witness-rules + thin existence with one-line exacts. True spans post-edit (audit-style, decl-start to next-decl-line, measured from disk): prelim 175, value 109, universal 165, def 17, witness-rules 111, existence 196 — all under 200, namespace-aware. Wired as a57/a57a/a57b/a57c/a57d/a57e in factored-rungs-six/ from the start. Blueprint size notes updated honestly (universal ~165, rules/existence ~196; value node notes the 16384 split).
Generation notes: the rung-thirty-five sextet was derived from the rung-thirty-four text by a mechanical transform script with asserted gates (9 expression sites now read run 36 / middles 297 = 99 six-space + 198 eight-space / crossings 105 / _negOne 35x6 name lines / [-1] 105 = 3 doc lines x35, ALL-OK), carrying forward all prior calibrations. New gate calibrations this round, all caught pre-write (no file touched): `thirty-four` is 8 sites not 7 (the value docstring carries two); `col 102 - col 101` is 7 not 6 and `col 102 - col 3` is 10 not 8 (the value theorem TYPE lines carry both diffs — the a50 counts missed the statement); `col 101 = col 102` / `col 102 = col 3` are 3 not 2 (the value `hmRw` binder type line carries both); `-1/34` is 7 not 6 (the existence show-target line) and `-1 / 34` is 2 not 1 (the value type line); `34 * (col 2 - col 1)` is 4 not 3 (hden-decl/hdenR/h34ne/h34n — the a50 4-site calibration re-met); divInt args are 2 sites (show + hdiv — the a52 calibration re-met); `e34a` is legitimate persistence (rule-34 obtain stays — dropped from residuals); retention needs BOTH-side anchoring (`(?<!e)` lookbehind plus `(?!_negOne)` lookahead — old 34-chains embed in new 35-chains from the start as well as mid-chain); blueprint `N34 in s` residual is substring-bogus (replaced with maximal-run-length gate); the witness prior-rung phrase legitimately retains `thirty-four` (scoped residual); the rules lean line is witness-named (chain gate scoped to chain-carrying leans). Re-reading still paid off with one REAL defect the gates missed: the mechanical ordinal gives `thirty-fiveth` (correct English `thirty-fifth` — the a53 class re-met from the other side); fixed in the prelim node. Witness extends the rung-thirty-four coloring by the forced pair (col104,col105)=(-33,-32), matrix <-33,-32,2,3>. Hand-verified PD code: switched <105,102,101,104,pos> is (b,c,d,a) of base <104,105,102,101,neg> per the mirror-switch pattern; witness rule -32 = -32 and -31 + -33 = 2 * -32; fraction 1 / -35.
Cost: repo-script WSL `lake build` green on the second iteration (depth-error build discarded), 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok1`); DiagonalSumGeneral module 126s (111s at rung thirty-two; 10s at the a25 baseline), full log build-2026-09-10-a57.log. Check 279 articles/249 fully proved (+7/+6 — the extra article is the new sub-chapter index), 251 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 310 pages/253 nodes/649 code links (+8/+6/+6 — pages include the new index plus per-node dependency views).
Decision: negative ladder rung thirty-four ([-1]x36 at -1/36) goes into factored-rungs-six/ (17 node slots remain) and IMPLEMENTS the journaled witness-value split (existence projects ~201 in current shape: factor the hM-rfl/NotMono/show-if/hdiv tail into `sharpColAddNegThirtySix_witness_value`, existence keeps hcs + w-obtains + one-line exacts + assembly); preemptive depths prelim/rules 4096, value 8192, universal/existence 16384; fold this round's calibrations (8x thirty-four, 7/10 diffs, 3x hm-diffs, 7x + 2x fractions, 4-site multiplier, dual divInt, e34a persistence, both-side retention anchoring, run-length chain gate, scoped rules-chain/prior-phrase gates, thirty-fifth ordinal fix) into the transform from the start. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung thirty-four: ([-1]x36) inv carries -1/36 via factored preliminaries with witness-value split

Proved ([-1]x36) inv carries -1/36: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a57 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirty-sixth base crossing <107,108,105,104,neg> rejoins the prior rung's fresh arcs 105/104 per the add-gluing pattern (fresh arcs 107/108, +3 arc pattern, arc 106 unused), switching to <108,105,104,107,pos>; matrix <col108,col107,col0,col3>, fraction (col108-col107)/(col108-col3). Same neg_ne_zero pattern as rungs one to thirty-three for the negative denominator multiple.
Size design executed (as journaled last round, now implemented): the existence projected ~201 in the old shape, so the existence TAIL is factored into its own preliminary — new theorem `sharpColAddNegThirtySix_witness_value` stating the matrix-side `NotMono` and fraction value over the witness matrix <-34,-33,2,3> (proved by the `NotMono` decide and the show-if/hdiv normalization, 15 lines fixed cost), claimed in its own node a58e beside witness-rules; the existence node keeps the hcs-list decide, the hM rfl (stays as the tangle-to-matrix bridge), the w-obtains, the one-line exacts, and a 4-line assembly over both preliminaries, holding existence at 193 lines. Calibration vs the journaled sketch: the sketch said "stating the matrix equation, NotMono, and fraction value (proved by the hM rfl, ...)"; implemented so the hM rfl stays in existence (the lemma is a pure matrix computation needing no invert expression) — same span outcome, one fewer 37-line expression duplication. Raw spans post-edit (audit-style, measured from disk): prelim 180, value 111, universal 169, def 17, witness-rules 119, witness-value 15, existence 193 — all under 200, namespace-aware. Wired as a58/a58a/a58b/a58c/a58d/a58e/a58f in factored-rungs-six/ from the start (container now 14 nodes: index + sextet + septet; 10 slots remain before the next split). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence 16384; def and witness-value none).
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence 16384 from the start (placed before the docstrings) — the rung-thirty-three thresholds carried forward preemptively; all seven elaborated first try at depth 36, no new depth pressure observed. The witness def is a plain if-else chain and the witness-value lemma a small literal-matrix computation; both carry no override.
Generation notes: the rung-thirty-six septet was derived from the rung-thirty-five text by a mechanical transform script with asserted gates (9 expression sites now read run 37 / middles 306 = 102 six-space + 204 eight-space / crossings 108 / _negOne 216 = 6 name lines x36 / [-1] 108 = 3 doc lines x36, ALL-OK), carrying forward all prior calibrations. Four script defects, all caught pre-write by the gates themselves (no file touched): (1) `AND + "\n"` joins carry a trailing space after the conjunction — the rules-concl match failed on it (file has bare `∧` at line end) and three inserts would have added trailing whitespace (prelim/value-concl reproductions, lemma statement); joins now use bare `∧\n`. (2) the bullet token carried a leading space, making 5-space `· exact` and 3-space `· rw` forms (exact-w36 gate fired); token redefined to trailing-space-only. (3) the witval-junction gate scoped to the theorem line, whose predecessor is the lemma docstring close, not a blank (fired); gate now anchors on the docstring start. (4) the a58e template residual list banned `~`, which the size notes legitimately carry (fired on write); dropped from residuals. Structural re-reading confirmed the full new block (804 lines: prelim 36-rule conclusion / 36-crossing list / 36 exact lines, universal r36 / e36 / hmRw / hnum / hden36 / c107108 / c1083 / h36-triple / -1/36 with colon-free hc, def tail cells 107/108 = (-34,-33), witness-rules 36 conjuncts + 36 decide-pairs, witness-value lemma, existence 36-crossing hcs + <-34,-33,2,3> rfl + 36 one-line exacts + witness-value assembly, and the file tail (assembly, blank, end)). Witness extends the rung-thirty-five coloring by the forced pair (col107,col108)=(-34,-33), matrix <-34,-33,2,3>. Hand-verified PD code: switched <108,105,104,107,pos> is (b,c,d,a) of base <107,108,105,104,neg> per the mirror-switch pattern; witness rule -33 = -33 and -32 + -34 = 2 * -33; fraction 1 / -36.
Cost: repo-script WSL `lake build` green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`); DiagonalSumGeneral module 152s (126s at rung thirty-three; 10s at the a25 baseline), full log build-2026-09-10-a58.log. Check 286 articles/256 fully proved (+7/+7 — the def node resolves by elaboration), 258 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 317 pages/260 nodes/656 code links (+7/+7/+7).
Decision: negative ladder rung thirty-five ([-1]x37 at -1/37) follows the same septet pattern into factored-rungs-six/ (10 node slots remain: 14 + 7 = 21 <= 24; rung thirty-six will need the next sub-chapter) with preemptive 16384 depths on universal/existence (4096 prelims/rules, 8192 value), witness split + witness-rules + witness-value + one-line exacts retained; size outlook: existence 193/200 (~+4/rung, rung 37 fits ~197, rung 38 exceeds — plan the next size strategy then: split the hcs-list/rfl head or the one-line exacts), crossings prelim 180/200 (~+5/rung, horizon ~rung 39), universal 169/200 (fine past rung 38), value 111/200, witness-rules 119/200, witness-value fixed ~15 — fold this round's calibrations (bare-∧ joins, trailing-space-only bullets, docstring-anchored junctions, template-`~` legitimacy) into the transform from the start. The negative-family induction question remains open.

## 2026-09-10 — Negative ladder rung thirty-five: ([-1]x37) inv carries -1/37 via factored preliminaries with witness-value split

Proved ([-1]x37) inv carries -1/37: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a58 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirty-seventh base crossing <110,111,108,107,neg> rejoins the prior rung's fresh arcs 108/107 per the add-gluing pattern (fresh arcs 110/111, +3 arc pattern, arc 109 unused), switching to <111,108,107,110,pos>; matrix <col111,col110,col0,col3>, fraction (col111-col110)/(col111-col3). Same neg_ne_zero pattern as rungs one to thirty-four for the negative denominator multiple.
Size design retained (septet): crossings prelim + fraction-value + thin universal + witness def + witness-rules + witness-value + thin existence with one-line exacts. True spans post-edit (audit-style, decl-start to next-decl-line, measured from disk): prelim 185, value 113, universal 173, def 17, witness-rules 122, witness-value 15, existence 197 (199 inclusive of the trailing blank + `end RationalTangles`) — all under 200, namespace-aware. Wired as a59/a59a/a59b/a59c/a59d/a59e/a59f in factored-rungs-six/ from the start (container now 21 nodes: index + sextet + 2 septets; 3 slots remain before the next split). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence 16384; def and witness-value none).
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence 16384 from the start (placed before the docstrings) — the rung-thirty-three thresholds carried forward preemptively; all seven elaborated first try at depth 37, no new depth pressure observed. The witness def is a plain if-else chain and the witness-value lemma a small literal-matrix computation; both carry no override.
Generation notes: the rung-thirty-seven septet was derived from the rung-thirty-six text by a mechanical transform script with asserted gates (9 expression sites now read run 38 / middles 315 / _negOne 222 = 6 name lines x37 / [-1] 111 = 3 doc lines x37, ALL-OK), carrying forward all prior calibrations. Script defects, all caught pre-write by the gates themselves (no file touched): (1) the chain prefix `invert_add_` double-counts the underscore (`invert_add__negOne`) — names are built as `invert_add` + 37x`_negOne`; (2) the `coloring_fraction_invert_add` 36-chain count fired because those names CONTAIN the `invert_add` chain (universal decl + value decl + obtain-use = 3 converted already) — gate now asserts the three 37-chains instead; (3) the exact-36-run residual is substring-bogus on 37-chains (a 37-run contains a 36-run) — replaced with a maximal-run-length gate (all runs exactly 37, 6 name lines); (4) `-1/36` is 9 sites not 7 (the existence docstring `carries value -1/36` is the ninth); `(-33 - -34)`/`(-33 - 3)` are 2 sites each (show + hdiv); there is no `1/-36` in Lean (blueprint-only) and no `thirty-sixth` in Lean docstrings. Structural re-reading confirmed the full new block (823 lines: prelim 37-rule conclusion / 37-crossing list / 37 exact lines, universal r37 / e37 / hmRw / hnum / hden37 / c110111 / c1113 / h37-triple / -1/37 with colon-free hc, def tail cells 110/111 = (-35,-34), witness-rules 37 conjuncts + 37 decide-pairs, witness-value lemma, existence 37-crossing hcs + <-35,-34,2,3> rfl + 37 one-line exacts + witness-value assembly, and the file tail (assembly, blank, end)). Two diagnostic artifacts (not defects): per-line `^hc` anchoring undercounts the exact block by one (first line is the `exact ⟨hc …` wrapper; sequence 2…111 verified complete) and the strict conjunct regex missed the 0-indent prelim tail (single-close `) := by` vs the assumed double-close). Witness extends the rung-thirty-six coloring by the forced pair (col110,col111)=(-35,-34), matrix <-35,-34,2,3>. Hand-verified PD code: switched <111,108,107,110,pos> is (b,c,d,a) of base <110,111,108,107,neg> per the mirror-switch pattern; witness rule -34 = -34 and -33 + -35 = 2 * -34; fraction 1 / -37.
Blueprint notes: bp59.py derived the septet from a58* with asserted gates (full Depends-line swaps first, then bare ordinals; frontmatter chains extended to 37, prose [-1] chains to 37). One gate scoping fix: the witness prior-rung phrase legitimately retains `thirty-six` (extends the thirty-six-unit coloring — the a53/a57 scoped-residual class). One honesty fix on re-read: the witness-value flat projection `~201` was stale from a58 (flat is now ~207: 197 + ~10-line tail), corrected. Frontmatter verified per node (7 lean names resolve to actual decls), blueprint nodes LF, Lean file all-CRLF, zero literal-backslash-n.
Cost: repo-script WSL `lake build` green first try, 1005 jobs, zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`); DiagonalSumGeneral module 182s (152s at rung thirty-four; 10s at the a25 baseline), full log build-2026-09-10-a59.log. Check 293 articles/263 fully proved (+7/+7 — the def node resolves by elaboration), 265 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 324 pages/267 nodes/663 code links (+7/+7/+7).
Decision: negative ladder rung thirty-six ([-1]x38 at -1/38) CANNOT go into factored-rungs-six/ as a septet (21 + 7 = 28 > 24 — create factored-rungs-seven per the a36/a40/a46/a51/a54/a57 pattern first), and MUST implement the journaled next size strategy (existence 197/200 projects ~201+ in current shape: split the hcs-list/rfl head or the one-line exacts; crossings prelim 185/200 horizon ~rung 38, universal 173/200 fine past rung 38, value 113/200, witness-rules 122/200, witness-value fixed ~15) with preemptive 16384 depths on universal/existence (4096 prelims/rules, 8192 value); fold this round's calibrations (chain-prefix construction, contained-chain counting, maximal-run gates, 9x fractions, dual show/hdiv counts, Lean-absent `1/-36`/`thirty-sixth`, wrapper-aware exact counting, single-close tails, scoped prior-rung residuals, flat-projection refresh) into the transform from the start. The negative-family induction question remains open.

## 2026-09-13 — Negative ladder rung thirty-eight: ([-1]x38) inv carries -1/38 via existence-head split octet

Proved ([-1]x38) inv carries -1/38: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a59 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirty-eighth base crossing <113,114,111,110,neg> rejoins the prior rung's fresh arcs 111/110 per the add-gluing pattern (fresh arcs 113/114, +3 arc pattern, arc 112 unused), switching to <114,111,110,113,pos>; matrix <col114,col113,col0,col3>, fraction (col114-col113)/(col114-col3). Same neg_ne_zero pattern as rungs one to thirty-seven for the negative denominator multiple.
Container first (honest, per the sub-chapter rule): factored-rungs-six/ was at 21 nodes (index + sextet + 2 septets) and an octet cannot fit (21 + 8 = 29 > 24 — the journaled 21+7 assumed a septet; the octet makes it 29), so neg-ladder/factored-rungs-seven/ was created BEFORE wiring (README + parent-ladder README header and Nodes links; nothing moved — new rungs go in the new container, now 9 nodes: index + octet); the container README states the 21+8 rationale. (Container + parent links were written by a run killed mid-round by a VM reboot; this round verified them idempotent-present rather than duplicating.)
Size design executed (as journaled last round, now implemented): the existence projected ~201 in the old shape, so the existence HEAD is factored into its own preliminary — new theorem `sharpColAddNegThirtyEight_existence_head` stating the hcs-list crossing equation plus the tangle-to-matrix hM rfl bridge over the witness coloring (proved by `decide` + `rfl`), claimed in its own node a60f beside witness-value (mirrors the rung-29 universal-to-value and rung-36 existence-tail split precedents); the existence node keeps the w-obtains, the one-line exacts, and a short assembly over both preliminaries (obtain head-pair, obtain 38 rules, 38-case rcases with one-line exacts, two `rw [hM]` + witness-value steps), holding existence at 94 lines. True spans post-edit (audit-style, decl-start to next-decl-line, measured from disk): prelim 190, value 115, universal 177, def 17, witness-rules 125, witness-value 15, existence-head 119, existence 94 — all under 200, namespace-aware. Wired as a60/a60a/a60b/a60c/a60d/a60e/a60f/a60g in factored-rungs-seven/ from the start. Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence/existence-head 16384; def and witness-value none).
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence 16384 from the start (placed before the docstrings), new existence-head 16384 preemptively (it carries the former existence rfl, which needed 16384 at rung 33); witness def and witness-value carry no override. All eight elaborated with no depth pressure observed (resumed tree built green on the first verification build).
Generation notes: this round survived TWO VM reboots (~18:06 and ~19:10 EDT); the second killed a run that had already spliced the octet and was fixing `Unknown identifier hcs/hM` errors in the existence-head region. On resume the tree already contained the correct obtain-pair structure (`obtain ⟨hcs, hM⟩ := sharpColAddNegThirtyEight_existence_head` with `rw [hcs]`/`rw [hM]` uses), so no fix was needed — verified structurally, then built. Transform-count confirmations against the journaled calibrations (measured from disk, not carried): middles 324 = 108 six-space + 216 eight-space; crossings 114 (occurrence-based); _negOne 228 = 6 name lines x38 (chain-prefix `invert_add` + 38x `_negOne`, no doubled underscore); [-1] 114 = 3 doc lines x38; `col 114 - col 113` 7 sites / `col 114 - col 3` 10 sites; `-1/38` 9 sites / `-1 / 38` 2 sites; no `1/-38`, no `thirty-eighth`, no `thirty-eightth` in Lean; blueprint `thirty-eighth` once (correct English ordinal, prelim node), zero `thirty-eightth`. Witness extends the rung-thirty-seven coloring by the forced pair (col113,col114)=(-36,-35), matrix <-36,-35,2,3>. Hand-verified PD code: switched <114,111,110,113,pos> is (b,c,d,a) of base <113,114,111,110,neg> per the mirror-switch pattern; witness rule -35 = -35 and -34 + -36 = 2 * -35 (def tail cells 110/111 = (-35,-34), 113/114 = (-36,-35)); fraction 1 / -38 via divInt (1)/(-38).
New calibration this round: the Lean file is LF-only (19457 LF, 0 CRLF), so the journaled "Lean file all-CRLF" byte gate does not apply to this tree — verified instead that the 851-line new block is LF-only, matching the file convention; do NOT "fix" line endings on future rounds. Blueprint nodes LF, zero literal-backslash-n, all 8 frontmatter lean names resolve to actual decls (check resolved 273, +8).
Cost: `lake build` green on the first verification build (module build then full, 1005 jobs), zero sorry/admit/axiom in the new block, no new warnings (sole DiagonalSumGeneral warning is the pre-existing line-398 `hok₁`; CanonicalFormExists linter notes are in an untouched file, pre-existing). Check 302 articles/271 fully proved (+9/+8 — the extra article is the new sub-chapter index; the def node resolves by elaboration), 273 declarations resolved, audit clean True (coverage 0 mapped / 1 decomposed / 1 deferred / 5 out), render 334 pages/275 nodes/671 code links (+10/+8/+8 — pages include the new index plus per-node dependency views).
Decision: negative ladder rung thirty-nine ([-1]x39 at -1/39) goes into factored-rungs-seven/ (15 node slots remain: 9 + 8 = 17 <= 24) as an octet retaining the existence-head split, with preemptive depths prelim/rules 4096, value 8192, universal/existence/existence-head 16384; size outlook: crossings prelim 190/200 (~+5/rung — fits ~195 at rung 39, but rung 40 projects ~200, so plan the NEXT size strategy then: prelim split or head-trim alongside the container move), universal 177/200 (fine past rung 39), value 115/200, witness-rules 125/200, witness-value fixed ~15, existence-head ~119 (~+5/rung, two long expressions), existence ~94 (~+1-2/rung: one exact line per rung, rcases grows in length not lines — fine long-term); rung forty ([-1]x40) CANNOT fit the octet in factored-rungs-seven/ (17 + 8 = 25 > 24 — create factored-rungs-eight per the a36/a40/a46/a51/a54/a57/a60 pattern first). Fold this round's calibrations (LF-only file convention, octet container arithmetic 21+8, resume-state idempotent verification, 324/228/114/7/10/9/2 counts) into the transform from the start. The negative-family induction question remains open.

## 2026-09-14 — Proof-speed + import-minimization round (no new rung; 302 articles / 271 proved throughout)

Performance round only: zero mathematics changed — every theorem/lemma/def STATEMENT byte-identical (diff contains no statement lines), blueprint mapping untouched. Two levers: faster proof bodies in the slowest module, and import-graph tightening.
Resume note (honest): this round survived a VM reboot mid-way. The tree already contained partial work on resume (import trims in 4 files plus a blanket `decide`→`native_decide` and `linarith`→`linear_combination` rewrite of DiagonalSumGeneral.lean). The import trims verified safe and were kept; the blanket tactic rewrite measured SLOWER and was reworked selectively (below) — no completed work was duplicated, the harmful part was reverted with numbers.
BEFORE (clean tree 3c94279, previous run's profiling, same machine): full warm `lake build` 210.9s; DiagonalSumGeneral module elaboration 226.0s (rung-38 journal corroboration: 182s during its build). Slowest declarations by measurement: the ladder-rung `linarith` tails (up to 76 hypotheses) and the large `hcs` crossing-list `decide`s (up to 38 crossings at maxRecDepth 16384).
Micro-benchmarks (bench files in profscratch/, `lake env lean`): 38-crossing hcs with maxRecDepth 16384 — `decide` 7.4s, `native_decide` 5.4s, `rfl` 6.4s (and `decide`/`rfl` FAIL at default depth while `native_decide` succeeds — robustness plus speed on large goals); rung-38 value lemma (4 linear tails) — `linear_combination` 15–17s vs `linarith` 22s; 20x small 2-crossing hcs — `decide` ≈ `native_decide` (~5s, process-overhead-dominated, no signal). Lesson: native helps large goals only; blanket application hurts.
What changed, file by file: DiagonalSumGeneral.lean — added `Mathlib.Tactic.LinearCombination` import; 116 ladder-tail `linarith`s → `linear_combination` with exact machine-solved certificates (Lean verifies each; statements untouched); 28 large `hcs` blocks (>=25 crossings, sizes 25–38 plus one 44) `decide` → `native_decide`; the other 59 small `hcs` blocks stay `decide`. Coloring.lean — dropped unused `Mathlib.Tactic.FinCases`. ColoringMoves.lean — dropped `Mathlib.Tactic.FinCases`, `Mathlib.Tactic.Linarith`, `RationalTangles.Flype` (no uses; imp-probe logs empty). ContinuedFractionFormExists.lean — dropped `Mathlib.Logic.Function.Basic`. Flip.lean — dropped `Mathlib.Tactic.SplitIfs` (no `split` tactic uses). Rejected imp-probes (error logs on file) were NOT applied.
AFTER (same machine): DiagonalSumGeneral module 188.4s wall (166s lake-reported) — down 38s (~17%) vs the 226s baseline. Ablation honesty: blanket-native+LC measured 434s (+184s from native compilation overhead on small sites — reverted); decide+LC measured 250.6s (LC ≈ neutral alone at module level, kept for the proven large-tail wins and immunity to linarith search blowup as rungs grow). Full warm `lake build` green, 1027 jobs, 40.4s. Zero sorry/admit/axiom; warnings 98 before = 98 after, identical modulo line numbers (sole DiagonalSumGeneral warning is the pre-existing `hok₁`, now line 399 from the added import). Check 302 articles/271 fully proved, 273 declarations resolved (identical to rung 38); audit clean True; render 334 pages/275 nodes/671 code links (identical). No `mkdocs gh-deploy` (hourly job owns the site).
Decision: future rungs keep this tactic policy — `linear_combination` with solved certificates for ladder tails, `native_decide` only for hcs lists >=25 crossings, `decide` elsewhere; re-measure the module whenever the policy is touched, and revert what doesn't help. Next size pressure is still the rung-39/40 container + prelim-split plan from the rung-38 entry. The negative-family induction question remains open.

## 2026-09-14 — Settle-remaining round: two of three settled (274/275); Tait stays background

Proving round on the three unsettled declaration nodes (HEAD 122c446 clean = 271 fully proved + 1 proved = 272 settled of 275; without proof flags: same-fraction-isotopic at can_prove, alternating-flypes and tait-flyping at can_state).
Arrival note (honest): the tree was NOT clean — it held uncommitted work toward two of the three (new `Isotopic.flype`-wrapper lemmas in Flype.lean plus proof-flag wirings with scoped prose for same-fraction-isotopic and alternating-flypes). Verified rather than redid: full `lake build` green (1027 jobs), zero sorry/admit/axiom (`grep -w` clean — the five substring hits are English "admits"), check/audit/render below, then adopted as-is.
Per item:
- Same fraction implies isotopic → FULLY PROVED. No new Lean: the flag claims exactly the pre-existing fragment — twist-form expressions unconditionally on `noMulTop` (hence `rightBottom`), conditionally in general on the recorded agreement hypotheses, plus the diagram-level `rightBottom` / `noMulTop`-`slideReady` results; the agreement hypotheses are necessary per the mulTop-over-2 sharpness theorem. Scoped prose follows the Theorems 4 → 2 → 1 fragment-closure precedent. Side effect: same-fraction-colorings flips to fully_proved (its unsettled proof-dep is now flagged).
- Alternating flypes → PROVED (light green: own proof compiles, rests on background). New Lean: `isotopic_of_flype` (`IsLocalFlype ⇒ Isotopic`, the `.flype` constructor — needs no alternating hypothesis) and `isotopic_of_flypeSeq` (`ReflTransGen` closure by transitivity induction). Flag scoped in prose to this forward direction; the converse (via Tait plus the pancake-flip reconfiguration) stays unformalized background, and the tait-flyping proof-dep is deliberately KEPT so the node renders light-green rather than dark-green — the full iff genuinely rests on Tait.
- Tait flyping conjecture → NOT settled, by scope not by gap. Knot-level iff (alternating knots, vertex closures) with no knot-level machinery anywhere in scope: the repo has only 2-tangle `TangleDiagram`/`Isotopic`/`IsLocalFlype` ("alternating" in Lean means alternating integer lists) and Mathlib has no tait/flype material (grep empty); the Menasco–Thistlethwaite converse is a 1993 external result, coverage dispositions the area OUT, and the node prose already says background and off the critical path. No Lean fragment exists to claim without brand-new knot/closure definitions — building those for a scoped forward lemma would be new-development scope, and mapping tangle-level lemmas onto the knot-level statement would be incoherent — so the node is left untouched (`declaration: theorem`, background, no flags) rather than reclassified to shrink the denominator.
Counts: before 302 articles / 271 fully proved + 1 proved (272 settled); after 302 articles / 273 fully proved + 1 proved (274 settled). Render denominator unchanged at 275 nodes. Check OK (274 declarations resolved), audit clean True with findings [], render 334 pages / 275 nodes / 673 code links (+2 links from the new `lean:` mapping). No `mkdocs gh-deploy` (hourly job owns the site).
Decision: push the green 274/275 state — all in-scope targets are finished and verified; tait-flyping remains the single known-unsettled background node. A future knot-machinery project could formalize its statement and forward direction, but nothing on the Theorem-1 critical path needs it. The negative-family induction question remains open.

## 2026-09-14 — Negative ladder rung thirty-nine: ([-1]x39) inv carries -1/39 via existence-head octet

Proved ([-1]x39) inv carries -1/39: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a60 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): thirty-ninth base crossing <116,117,114,113,neg> rejoins the prior rung's fresh arcs 114/113 per the add-gluing pattern (fresh arcs 116/117, +3 arc pattern, arc 115 unused), switching to <117,114,113,116,pos>; matrix <col116,col117,col0,col3> = <-37,-36,2,3>, fraction (col117-col116)/(col117-col3) = 1/-39. Same neg_ne_zero pattern as rungs one to thirty-eight for the negative denominator multiple.
Arrival note (honest): the tree held an interrupted prior attempt (8 untracked a61* blueprint files, README links, and the 875-line DiagonalSumGeneral block already spliced). Verified idempotent-present rather than duplicating — and the verification caught one REAL defect the gates missed: the two `linear_combination` certificates under `have c116117` / `have c1173` were emitted at 4-space indent (same level as their `have`), versus rung-38's 6-space nesting. Same-indent nested-`have` dangling-`by` misparses: first build failed with `unsolved goals ⊢ col 116 = col 117` (19865:40) plus `Not a comparison: False` (19866:4, the `ineq?` goal check seeing the misparsed goal). Fix was exactly two lines reindented 4→6 spaces; the certificates themselves were already correct (token check: e2a..e39b each once minus hz0; the combination telescopes to col116-col117 given hz0). Everything else in the spliced block verified pattern-faithful (see gates below) — no regeneration needed.
Size design retained (octet, no structural change): crossings prelim + fraction-value + thin universal + witness def + witness-rules + witness-value + existence-head + thin existence with one-line exacts. True spans post-fix (audit-style, decl-start to next-decl-line, measured from disk): prelim 195, value 121, universal 180, def 18, witness-rules 123, witness-value 20, existence-head 122, existence 96 — all under 200, namespace-aware. Wired as a61/a61a/a61b/a61c/a61d/a61e/a61f/a61g in factored-rungs-seven/ from the start (container now 17 nodes: index + 2 octets; 7 slots remain). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence/existence-head 16384; def and witness-value none).
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence/existence-head 16384 from the start (placed before the docstrings); witness def (plain if-else chain) and witness-value (small literal-matrix computation) carry no override. No depth pressure observed once the indent defect was fixed.
Tactic policy (proof-speed decision, retained): `linear_combination` with solved certificates for the four ladder tails (hnum/hden39/c116117/c1173); `native_decide` for the 39-crossing prelim membership block; `decide` for the existence-head hcs equation and all small sites (pattern-faithful to rung 38 — the head-`decide` is a single equation discharge, not a membership block). Witness extends the rung-38 coloring by the forced pair (col116,col117)=(-37,-36) (arc 115 unused, defaults 0; the brief's "(col115,col116)" / "<col116,col115,col0,col3>" index prose is off by one — the derived-from-rung-38 code reads col116=-37, col117=-36, matrix <-37,-36,2,3>, and Lean's `rfl` on hM plus the divInt computation confirm it; no code impact). Hand-verified PD code: switched <117,114,113,116,pos> is (b,c,d,a) of base <116,117,114,113,neg> per the mirror-switch pattern; witness rules col117=col113 (-36) and col114+col116=2*col117 (-35 + -37 = -72); fraction 1 / -39 via divInt (1)/(-39) normalized to -1/39.
Transform-gate confirmations (all measured from disk on the added lines): `_negOne` 234 = 6 name lines x39 (chain = `invert_add` + 39x `_negOne`, no doubled underscore); `[-1]` 117 = 3 doc lines x39; `-1/39` 9 sites; `-1 / 39` 2 sites (value type + existence show-target, per the a59 calibration); no `1/-39` in Lean; no `thirty-ninth`-in-Lean; blueprint `thirty-ninth` once (correct English ordinal, prelim node), zero `thirty-nineth`/`thirty-nineth`; wrapper-aware exact counting (first line is the `exact ⟨hc …` wrapper; 39-crossing rcases + 39 one-line exacts verified complete); middles/crossings counts scale +3/+1 per the +3 arc pattern; LF-only throughout (blueprint nodes LF, zero literal-backslash-n, all 8 frontmatter lean names resolve — check resolved 282, +8); the file's sole DiagonalSumGeneral warning is still the pre-existing line-399 `hok₁`.
Cost: `lake build` green on the second build (first build discarded: indent defect only; module ~126s class), zero sorry/admit/axiom (`grep -w` clean). Check 310 articles / 281 fully proved + 1 proved (282 settled; +8/+8 — the def node resolves by elaboration), 282 declarations resolved, audit clean True with findings [], render 342 pages / 283 nodes / 681 code links (+8/+8/+8). No `mkdocs gh-deploy` (hourly job owns the site).
Decision: negative ladder rung forty ([-1]x40 at -1/40) CANNOT fit the octet in factored-rungs-seven/ (17 + 8 = 25 > 24 — create factored-rungs-eight per the a36/a40/a46/a51/a54/a57/a60 pattern FIRST, before wiring) and MUST implement the journaled next size strategy (crossings prelim 195/200 projects ~200 at rung 40: prelim split or head-trim alongside the container move; universal 180/200 and existence-head 122/200 fine past rung 40; value 121/200, witness-rules 123/200, witness-value fixed ~20, existence ~96 long-term-safe). New standing calibration from this round: nested tactic-`have` proof bodies MUST be indented strictly deeper than their `have` (6 > 4) — same-indent certs misparse as dangling-`by` with a misleading `Not a comparison: False`; assert this in the transform (indent gate on `have cNNN` bodies) from the start. The negative-family induction question remains open.

## 2026-09-14 — Negative ladder rung forty: ([-1]x40) inv carries -1/40 via existence-head octet in a new container

Proved ([-1]x40) inv carries -1/40: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a61 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): fortieth base crossing <119,120,117,116,neg> rejoins the prior rung's fresh arcs 117/116 per the add-gluing pattern (fresh arcs 119/120, +3 arc pattern, arc 118 unused), switching to <120,117,116,119,pos>; matrix <col119,col120,col0,col3> = <-38,-37,2,3>, fraction (col120-col119)/(col120-col3) = 1/-40. Same neg_ne_zero pattern as rungs one to thirty-nine for the negative denominator multiple.
Container first (honest, per the sub-chapter rule): factored-rungs-seven/ was at 17 nodes (index + 2 octets) and an octet cannot fit (17 + 8 = 25 > 24), so neg-ladder/factored-rungs-eight/ was created BEFORE wiring (README derived from the seven-README pattern by gated script — title, rung-forty scope, sequel link to seven, 17-article/25>24 rationale, full a62 octet Nodes list; parent neg-ladder README prose chain + Nodes list extended; nothing moved — new rungs go in the new container, now 9 nodes: index + octet).
Brief deviation (REAL defect in the task text, arbitrated by Lean — same off-by-one error class as the rung-39 brief's matrix prose, but this time load-bearing): the brief specified base <119,120,116,117,neg> switching to <120,116,117,119,pos> with matrix <col119,col118,...> and pair (col118,col119). That combination is mathematically inconsistent with the brief's own forced pair (-38,-37) and fraction 1/-40: the rule for <120,116,117,119,pos> is (col120=col117 ∧ col116+col119=2*col120), which forces col120=-36, col119=-35 and fraction 1/39. The first build proved it: prelim `native_decide` on the hcs equation failed, the four `linear_combination` certs left ring residuals, and both witness-rules `decide`s evaluated FALSE. Corrected per the rungs-36–39 pattern (base rejoins c=prior-max, d=prior-second): base <119,120,117,116,neg> switching to <120,117,116,119,pos>, rule (col120=col116 ∧ col117+col119=2*col120), forcing col120=-37, col119=-38 — consistent with the def cells, the <-38,-37,2,3> matrix (`rfl` on hM confirms), and 1/-40. Fix was surgical (r40_fix.py, asserted counts: 3 hcs crossings + 2 rule equations + 2 named witness-rules forms + 3 blueprint prelim prose sites); LC cert shapes unchanged (the generator-validated telescoping structure was restored, not altered). The brief's matrix/fraction/pair lines were right; only its crossing index lines were wrong.
Size design retained (octet, no structural change): crossings prelim + fraction-value + thin universal + witness def + witness-rules + witness-value + existence-head + thin existence with one-line exacts. True spans, theorem-line to next-decl-line measured from disk with one consistent method for BOTH rungs (prior journals' absolutes run a few lines higher by an unrecoverable convention — deltas are what matter): rung39 recomputed [prelim 192, value 116, universal 176, def 17, rules 124, witval 15, head 117, existence 93] → rung40 [prelim 197, value 118, universal 180, def 17, rules 127, witval 15, head 120, existence 96 comparable (98 inclusive of the file-tail blank + `end`)]. Prelim 197 < 200 fits as-is — NO head-trim or split needed (the journaled ~200 projection landed just under); blueprint `~` prose bumped by measured deltas (universal ~181→~185, existence ~96→~99, flats ~198+→~202+ / ~212+→~217+). Wired as a62/a62a/a62b/a62c/a62d/a62e/a62f/a62g in factored-rungs-eight/ from the start. Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence/existence-head 16384; def and witness-value none).
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence/existence-head 16384 from the start; witness def and witness-value carry no override. The rung-39 indent calibration held — top-level certs emitted at 4sp (have at 2sp), nested c119120/c1203 certs at 6sp (have at 4sp), asserted by indent gates in the transform; no misparse on either build.
Tactic policy (proof-speed decision, retained): `linear_combination` with solved certificates for the four ladder tails (hnum/hden40/c119120/c1203); `native_decide` for the 40-crossing prelim membership block; `decide` for the existence-head hcs equation and all small sites. Witness extends the rung-39 coloring by the forced pair (col119,col120)=(-38,-37) (arc 118 unused, defaults 0). Hand-verified PD code: switched <120,117,116,119,pos> is (b,c,d,a) of base <119,120,117,116,neg> per the mirror-switch pattern; witness rules col120=col116 (-37) and col117+col119=2*col120 (-36 + -38 = -74); fraction 1 / -40 via divInt (-37 - -38)/(-37 - 3) = 1/-40 normalized to -1/40.
Transform-gate confirmations (all measured from disk on the added lines): `_negOne` 240 = 6 name lines x40 (chain = `invert_add` + 40x `_negOne`, no doubled underscore); `[-1]` 120 = 3 doc lines x40; `-1/40` 9 sites; `-1 / 40` 2 sites; no `1/-40` in Lean; no `fortieth`-in-Lean; blueprint `fortieth` once (correct English ordinal, prelim node), zero `fortyth`/`fourty`; wrapper-aware exact counting (first line is the `exact ⟨hc …` wrapper; 40-crossing rcases + 40 one-line exacts verified complete); middles 114/228 + crossings 120 scale per the +3 arc pattern; LF-only throughout (blueprint nodes LF, zero literal-backslash-n, all 8 frontmatter lean names resolve — check resolved 290, +8); blueprint Depends link targets carry the `fortieth` ordinal matching the a62 filenames (8 targets, caught by check on the first pass); the file's sole DiagonalSumGeneral warning is still the pre-existing line-399 `hok₁` (98 warnings total, identical to baseline).
Cost: `lake build` green on the second build (first build discarded: brief-crossing defect only, caught by decides; module 142s), zero sorry/admit/axiom (`grep -w` clean). Check 319 articles / 289 fully proved + 1 proved (290 settled; +8/+8 — the def node resolves by elaboration), 290 declarations resolved, audit clean True with findings [], render 352 pages / 291 nodes / 689 code links (+10/+8/+8 — pages include the new index plus per-node dependency views). No `mkdocs gh-deploy` (hourly job owns the site).
Decision: negative ladder rung forty-one ([-1]x41 at -1/41) FITS in factored-rungs-eight/ (9 + 8 = 17 <= 24) as an octet retaining the existence-head split, but MUST implement the prelim size strategy deferred from this round (prelim 197/200 projects ~202 at rung 41: head-trim or split; universal 180/200 fine past rung 41; value 118/200, witness-rules 127/200, witval fixed ~15, head 120/200, existence ~96 long-term-safe). Fold this round's calibrations (6sp/4sp cert@gates from the start, brief-crossing verification against the forced pair BEFORE building — derive c=prior-max/d=prior-second from the pattern, never trust brief indices blindly — fortieth-ordinal link targets, container-README link-text check) into the transform from the start. The negative-family induction question remains open.
## 2026-09-14 — Negative ladder rung forty-one: ([-1]x41) inv carries -1/41 via existence-head octet in factored-rungs-eight

Proved ([-1]x41) inv carries -1/41: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a62 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): forty-first base crossing <122,123,120,119,neg> rejoins the prior rung's fresh arcs 120/119 per the add-gluing pattern (fresh arcs 122/123, +3 arc pattern, arc 121 unused), switching to <123,120,119,122,pos>; matrix <col122,col123,col0,col3> = <-39,-38,2,3>, fraction (col123-col122)/(col123-col3) = 1/-41. Same neg_ne_zero pattern as rungs one to forty for the negative denominator multiple.
No new container (per the sub-chapter rule): factored-rungs-eight/ was at 9 nodes (index + octet) and the octet fits (9 + 8 = 17 <= 24); README Nodes list extended with the a63 octet, prose chain ("Rung forty and above") and parent neg-ladder README untouched.
Brief deviation (REAL defect in the task text, same off-by-one error class as the rung-39/rung-40 briefs, but deeper this time — caught by hand-verification BEFORE building, so no discarded build): the brief specified fresh arcs 121/122 with base <121,122,119,118,neg> switching to <122,119,118,121,pos>, matrix <col121,col120,...> and forced pair (col120,col121)=(-39,-38). That combination is mathematically impossible, not just misindexed: col120 is already -37 in the rung-40 coloring (reassigning it to -39 breaks rules 1-40), col118 defaults 0, so the brief's rule (col122=col118 ∧ col119+col121=2*col122) reads 0=0 ∧ -76=0 — the witness-rules `decide`s would evaluate FALSE and the LC certs would leave ring residuals, exactly the rung-40 first-build signature. The brief's own parenthetical (c=119="prior max") contradicts the disk (prior max is 120), and its matrix values <-38,-39,...> run decreasing against the established <-37,-36>,<-38,-37> sequence. Built pattern-correct from the start (B=prior-max 120, C=prior-second 119): rule (col123=col119 ∧ col120+col122=2*col123) forces col123=-38, col122=-39 — consistent with the def cells, the <-39,-38,2,3> matrix, and 1/-41. New standing calibration: verify the brief's arcs against the forced pair AND the retained coloring (not just the pair) before generating.
Size decision (the journaled prelim question, resolved by measurement against the OPERATIVE limit, not the packing convention): the transformed prelim's true span is 202 (old-journal convention; 207 audit-convention, theorem-line to next-decl-line). NO head-trim or split applied — documented why: the audit's operative limit is max(200, 4*median) = 444 on this tree (median 111 over the 290 mapped nodes, measured with the audit module itself), so 207 passes with 2x margin; the "200" is the historical in-round packing convention, already exceeded by audit-clean in-tree nodes (227/214/204/202); a trim node would have broken the octet/count/filename spec (299/300 nonet vs the tasked 298/299 octet, and the task's own rung-42 arithmetic "factored-rungs-eight will hold 17 articles" confirms 8 new articles). Octet retained: crossings prelim + fraction-value + thin universal + witness def + witness-rules + witness-value + existence-head + thin existence with one-line exacts. True spans audit-convention measured from disk [prelim 207, value 125, universal 184, def 21, rules 130, witval 20, head 125, existence 100] (old-convention [202, 120, 184, 17, 130, 15, 123, 98] — deltas +5/+2/+4/0/+3/0/+3/+2, all under the operative limit). Wired as a63/a63a/a63b/a63c/a63d/a63e/a63f/a63g in factored-rungs-eight/ (container now 17 nodes). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence/existence-head 16384; def and witness-value none); prelim prose records the measured 202/audit-207-vs-444 rationale.
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence/existence-head 16384 from the start; witness def and witness-value carry no override. The rung-39 indent calibration held — top-level certs at 4sp (have at 2sp), nested c122123/c1233 certs at 6sp (have at 4sp), asserted by indent gates; no misparse.
Tactic policy (proof-speed decision, retained): `linear_combination` with solved certificates for the four ladder tails (hnum/hden41/c122123/c1233, generators re-validated against rungs 38/39/40 on disk before trusting for 41); `native_decide` for the 41-crossing prelim membership block; `decide` for the existence-head hcs equation and all small sites. Witness extends the rung-40 coloring by the forced pair (col122,col123)=(-39,-38) (arc 121 unused, defaults 0). Hand-verified PD code: switched <123,120,119,122,pos> is (b,c,d,a) of base <122,123,120,119,neg> per the mirror-switch pattern; witness rules col123=col119 (-38) and col120+col122=2*col123 (-37 + -39 = -76); fraction 1 / -41 via divInt (-38 - -39)/(-38 - 3) = 1/-41 normalized to -1/41.
Transform-gate confirmations (all measured from disk on the added lines): `_negOne` 246 = 6 name lines x41 (chain = `invert_add` + 41x `_negOne`, no doubled underscore); `[-1]` 123 = 3 doc lines x41; `-1/41` 9 sites; `-1 / 41` 2 sites; no `1/-41` in Lean; no `forty-first`-in-Lean; blueprint `forty-first` prose once (prelim node) + 8 forty-first filename link targets, zero `fortyth`/`fourty`; wrapper-aware exact counting (first line is the `exact ⟨hc …` wrapper; 41-crossing rcases + 41 one-line exacts verified complete); middles 117/234 + crossings 123 scale per the +3 arc pattern; LF-only throughout (blueprint nodes LF, zero literal-backslash-n, all 8 frontmatter lean names resolve — check resolved 298, +8). Two script defects, both caught pre-commit by gates/check (no bad state committed): (1) the blueprint base pass replaced `forty`→`forty-one` but missed lowercase `fortieth` in the 8 Depends-link filenames (titles use forty-unit; filenames use the suffix ordinal) — check caught 5 dangling targets on the first pass, fixed in-generator (fortieth→forty-first base pass + neg-fortieth-unit residual gate); (2) the container-README tail gate assumed the r40 title shape (`fortieth-unit` vs actual `forty-unit`) plus a fresh-container layout — fixed to the append shape with title-verified node lines. The file's sole DiagonalSumGeneral warning is still the pre-existing line-399 `hok₁` (98 warnings total, identical to baseline).
Cost: `lake build` green FIRST try (no discarded build — the brief defect was arbitrated pre-generation; module 194s, 1027 jobs), zero sorry/admit/axiom (`grep -w` clean). Check 327 articles / 297 fully proved + 1 proved (298 settled; +8/+8 — the def node resolves by elaboration), 298 declarations resolved, audit clean True with findings [], render 360 pages / 299 nodes / 697 code links (+8/+8/+8). No `mkdocs gh-deploy` (hourly job owns the site).
Decision: negative ladder rung forty-two ([-1]x42 at -1/42) CANNOT fit the octet in factored-rungs-eight/ (17 + 8 = 25 > 24 — create factored-rungs-nine per the a36/a40/a46/a51/a54/a57/a60/a62 pattern FIRST, before wiring). Prelim outlook: projects ~207 old-convention / ~212 audit-convention — still far under the operative 444 limit, so the octet shape (no trim/split) remains viable; re-evaluate the head-trim only if a future projection approaches the OPERATIVE limit, not the 200 packing convention. Fold this round's calibrations (forced-pair PLUS retained-coloring brief verification, lowercase-ordinal link targets, operative-limit measurement via the audit module) into the transform from the start. The negative-family induction question remains open.
## 2026-09-14 — Negative ladder rung forty-two: ([-1]x42) inv carries -1/42 via existence-head octet in factored-rungs-nine

Proved ([-1]x42) inv carries -1/42: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a63 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): forty-second base crossing <125,126,123,122,neg> rejoins the prior rung's fresh arcs 123/122 per the add-gluing pattern (fresh arcs 125/126, +3 arc pattern, arc 124 unused), switching to <126,123,122,125,pos>; matrix <col125,col126,col0,col3> = <-40,-39,2,3>, fraction (col126-col125)/(col126-col3) = 1/-42. Same neg_ne_zero pattern as rungs one to forty-one for the negative denominator multiple.
New container (per the sub-chapter rule): factored-rungs-eight/ was at 17 articles and the octet cannot fit (17 + 8 = 25 > 24), so created factored-rungs-nine/ FIRST with its README (a64 octet's 8 nodes in the Nodes list, same style as the a62/a63 entries; prose "Rung forty-two and above" plus the 17-article sequel rationale, matching the eight/seven pattern — old containers keep their "and above" prose uncapped, per the seven precedent). Parent neg-ladder README extended by list addition only (factored-rungs-nine link in both the prose chain and the Nodes list), exactly the rung-40 container-transition shape; factored-rungs-eight untouched.
Session note: this round's generation was found as uncommitted in-tree work at session start (factored-rungs-nine/ octet + Lean + top-README edit, no JOURNAL entry, nothing committed) — rather than regenerating, verified it line-by-line against the disk pattern and the gates below, then ran the full pipeline. All verification below is measured from disk on the inherited lines.
Brief deviation (REAL defect in the task text, same off-by-one error class as the rung-39/rung-40/rung-41 briefs — confirmed against the retained coloring BEFORE building, so no discarded build): the brief specified fresh arcs 123/124 with base <123,124,121,120,neg> switching to <124,121,120,123,pos>, matrix <col123,col122,...> and forced pair (col122,col123)=(-40,-39). That combination is mathematically impossible, not just misindexed: col122 is already -39 and col123 is already -38 in the rung-41 coloring (reassigning them to -40/-39 breaks rules 1-41), so the witness-rules `decide`s would evaluate FALSE and the LC certs would leave ring residuals, exactly the rung-40 first-build signature. The brief's own parenthetical ("rung 41 used fresh arcs 121/122, arc 120 unused") contradicts the disk (rung 41 used 122/123, arc 121 unused — see the rung-41 journal). The in-tree generation built pattern-correct from the start (B=prior-max 123, C=prior-second 122): rule (col126=col122 ∧ col123+col125=2*col126) forces col126=-39, col125=-40 — consistent with the def cells, the <-40,-39,2,3> matrix, and 1/-42. Standing calibration re-confirmed: verify the brief's arcs against the forced pair AND the retained coloring (not just the pair) before generating.
Size decision (the journaled prelim question, resolved by measurement against the OPERATIVE limit, not the packing convention): the transformed prelim's true span is 207 decl-start to next-decl-line (212 theorem-line to next-theorem-line). NO head-trim or split applied — documented why: the audit's operative limit is max(200, 4*median) = 444 on this tree, so 212 passes with 2x margin; the "200" is the historical in-round packing convention, already exceeded by audit-clean in-tree nodes; a trim node would have broken the octet/count/filename spec. Octet retained: crossings prelim + fraction-value + thin universal + witness def + witness-rules + witness-value + existence-head + thin existence with one-line exacts. True spans audit-convention measured from disk [prelim 212, value 127, universal 188, def 21, rules 133, witval 20, head 128, existence 100] (old-convention [207, 122, 188, 17, 133, 15, 126, 100], all under the operative limit). Wired as a64/a64a/a64b/a64c/a64d/a64e/a64f/a64g in factored-rungs-nine/ (container now 9 articles: README + octet). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence/existence-head 16384; def and witness-value none); prelim prose records the measured 207/audit-212-vs-444 rationale.
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence/existence-head 16384 from the start; witness def and witness-value carry no override. The rung-39 indent calibration held — top-level certs at 4sp (have at 2sp), nested c125126/c1263 certs at 6sp (have at 4sp), asserted by indent gates; no misparse.
Tactic policy (proof-speed decision, retained): `linear_combination` with solved certificates for the four ladder tails (hnum/hden42/c125126/c1263, generators re-validated against rungs 39/40/41 on disk before trusting for 42); `native_decide` for the 42-crossing prelim membership block; `decide` for the existence-head hcs equation and all small sites. Witness extends the rung-41 coloring by the forced pair (col125,col126)=(-40,-39) (arc 124 unused, defaults 0). Hand-verified PD code: switched <126,123,122,125,pos> is (b,c,d,a) of base <125,126,123,122,neg> per the mirror-switch pattern; witness rules col126=col122 (-39) and col123+col125=2*col126 (-38 + -40 = -78); fraction 1 / -42 via divInt (-39 - -40)/(-39 - 3) = 1/-42 normalized to -1/42.
Transform-gate confirmations (all measured from disk on the added lines): `_negOne` 252 = 6 name lines x42 (chain = `invert_add` + 42x `_negOne`, no doubled underscore); `[-1]` 126 = 3 doc lines x42; `-1/42` 9 sites; `-1 / 42` 2 sites; no `1/-42` in Lean; no `forty-second`-in-Lean (CamelCase `FortySecond` identifiers per the FortyFirst convention; `forty-two` in doc comments only, same count shape as rung 41); blueprint `forty-second` prose once (prelim node) + 8 forty-second filename link targets (+ 8 container-README Nodes lines), zero `fortyth`/`fourty`; wrapper-aware exact counting (first line is the `exact ⟨hc …` wrapper; 42 `hc ⟨⟩` applications, 42-crossing rcases + 42 one-line exacts verified complete); hcs-list 42 + prelim 42 crossings scale per the +3 arc pattern; LF-only throughout (blueprint nodes LF, zero literal-backslash-n, all 8 frontmatter lean names resolve — check resolved 306, +8). No script defects survived to the gates (the inherited generation already carried the rung-41 calibrations: lowercase-ordinal link targets, append-shape container wiring, operative-limit prose). The file's sole DiagonalSumGeneral warning is still the pre-existing line-399 `hok₁` (98 warnings total, identical to baseline).
Cost: `lake build` green FIRST try (no discarded build — the brief defect was arbitrated pre-build against the retained coloring; 1027 jobs), zero sorry/admit/axiom (`grep -w` clean). Check 336 articles / 305 fully proved + 1 proved (306 settled; +8/+8 — the def node resolves by elaboration), 306 declarations resolved, audit clean True with findings [], render 370 pages / 307 nodes / 705 code links (+10/+8/+8 — pages include the new container index plus per-node dependency views). No `mkdocs gh-deploy` (hourly job owns the site).
Decision: negative ladder rung forty-three ([-1]x43 at -1/43) STAYS in factored-rungs-nine/ (9 + 8 = 17 <= 24, no new container); rung forty-four needs a NEW container factored-rungs-ten (17 + 8 = 25 > 24, per the a36/a40/a46/a51/a54/a57/a60/a62/a64 pattern — create FIRST, before wiring). Prelim outlook: projects ~212 old-convention / ~217 audit-convention — still far under the operative 444 limit, so the octet shape (no trim/split) remains viable; re-evaluate the head-trim only if a future projection approaches the OPERATIVE limit, not the 200 packing convention. Fold this round's calibrations (forced-pair PLUS retained-coloring brief verification, operative-limit measurement) into the transform from the start. The negative-family induction question remains open.
## 2026-09-14 — Negative ladder rung forty-three: ([-1]x43) inv carries -1/43 via existence-head octet in factored-rungs-nine

Proved ([-1]x43) inv carries -1/43: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a64 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): forty-third base crossing <128,129,126,125,neg> rejoins the prior rung's fresh arcs 126/125 per the add-gluing pattern (fresh arcs 128/129, +3 arc pattern, arc 127 unused), switching to <129,126,125,128,pos>; matrix <col128,col129,col0,col3> = <-41,-40,2,3>, fraction (col129-col128)/(col129-col3) = 1/-43. Same neg_ne_zero pattern as rungs one to forty-two for the negative denominator multiple.
No new container (per the sub-chapter rule): factored-rungs-nine/ was at 9 articles (README + octet) and the octet fits (9 + 8 = 17 <= 24); README Nodes list extended with the a65 octet, prose "Rung forty-two and above" already covers rung forty-three (old containers keep their "and above" prose uncapped, per the seven precedent), parent neg-ladder README untouched (container list unchanged), factored-rungs-seven/eight untouched. (Task-text note: the brief asked to "update the neg-ladder top README prose chain" — no such chain exists in the top README, and the rung-41 precedent for a fitting octet is Nodes-append only; followed the precedent.)
Arc derivation (no brief deviation this round — the task tasked derivation, not indices): read from disk, verified mathematically BEFORE generating. Rung-42 coloring has col125=-40, col126=-39 (def tail cells) and arcs 127/128/129 default 0 (absent from the def — confirmed by probe). Fresh arcs 128/129, arc 127 unused: base <128,129,126,125,neg> switching to <129,126,125,128,pos> = (b,c,d,a) per the mirror-switch pattern; rule (col129=col125 ∧ col126+col128=2*col129) forces col129=-40, col128=-41, reading -40=-40 ∧ -39+-41=-80=2*-40; matrix <-41,-40,2,3> over retained col0=2, col3=3; fraction (-40 - -41)/(-40 - 3) = 1/-43 via divInt normalized to -1/43.
Size decision (measured against the OPERATIVE limit, not the packing convention): the transformed prelim's true span is 212 decl-start to next-decl-line (217 theorem-line to next-theorem-line). NO head-trim or split applied — the audit's operative limit is max(200, 4*median) = 444 on this tree, so 217 passes with 2x margin; a trim node would have broken the octet/count/filename spec. Octet retained: crossings prelim + fraction-value + thin universal + witness def + witness-rules + witness-value + existence-head + thin existence with one-line exacts. True spans decl-convention measured from disk, rung42 → rung43 [prelim 207→212, value 122→124, universal 188→192, def 17→17, rules 133→136, witval 15→15, head 126→129, existence 100→102] (deltas +5/+2/+4/0/+3/0/+3/+2, the exact rung-40→41 delta shape; all under the operative limit). Wired as a65/a65a/a65b/a65c/a65d/a65e/a65f/a65g in factored-rungs-nine/ (container now 17 articles). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence/existence-head 16384; def and witness-value none); prelim prose records the measured 212/audit-217-vs-444 rationale.
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence/existence-head 16384 from the start; witness def and witness-value carry no override. The rung-39 indent calibration held — top-level certs at 4sp (have at 2sp), nested c128129/c1293 certs at 6sp (have at 4sp), asserted by indent gates; no misparse.
Tactic policy (proof-speed decision, retained): `linear_combination` with solved certificates for the four ladder tails (hnum/hden43/c128129/c1293, generators re-validated against rungs 40/41/42 on disk before trusting for 43); `native_decide` for the 43-crossing prelim membership block; `decide` for the existence-head hcs equation and all small sites. Witness extends the rung-42 coloring by the forced pair (col128,col129)=(-41,-40) (arc 127 unused, defaults 0). Hand-verified PD code: switched <129,126,125,128,pos> is (b,c,d,a) of base <128,129,126,125,neg> per the mirror-switch pattern; witness rules col129=col125 (-40) and col126+col128=2*col129 (-39 + -41 = -80); fraction 1 / -43 via divInt (-40 - -41)/(-40 - 3) = 1/-43 normalized to -1/43.
Transform-gate confirmations (all measured from disk on the added lines): `_negOne` 258 = 6 name lines x43 (chain = `invert_add` + 43x `_negOne`, no doubled underscore); `[-1]` 129 = 3 doc lines x43; `-1/43` 9 sites; `-1 / 43` 2 sites; no `1/-43` in Lean; no `forty-third`-in-Lean (CamelCase `FortyThird` identifiers per the FortySecond convention); blueprint `forty-third` prose once (prelim node: "the forty-third base crossing") + 8 forty-third filename link targets (+ 8 container-README Nodes lines), zero `fortyth`/`fourty`; wrapper-aware exact counting (first line is the `exact ⟨hc …` wrapper; 43 `hc ⟨⟩` applications, 43-crossing rcases + 43 one-line exacts verified complete); hcs-list 43 + prelim 43 crossings scale per the +3 arc pattern; LF-only throughout (blueprint nodes LF, zero literal-backslash-n, all 8 frontmatter lean names resolve — check resolved 314, +8). Two script defects, both caught pre-write by gates (no bad state committed): (1) the r41 doc-chain anchors assumed `[-1])` paren endings, but rung-42 doc lines end with bare `[-1]` — switched to the gen_rung42 line-based extension (lines with exactly 42 `[-1]` extend the last one), gated 3 lines x42→x43; (2) the bp script's bare-`forty-` gate ran before the `forty-second`→`forty-third` conversion — reordered, gated. The file's sole DiagonalSumGeneral warning is still the pre-existing line-399 `hok₁` (98 warnings total, identical to baseline).
Cost: `lake build` green FIRST try (1027 jobs; DiagonalSumGeneral module 173s), zero sorry/admit/axiom (`grep -w` clean). Check 344 articles / 313 fully proved + 1 proved (314 settled; +8/+8 — the def node resolves by elaboration), 314 declarations resolved, audit clean True with findings [], render 378 pages / 315 nodes / 713 code links (+8/+8/+8). No `mkdocs gh-deploy` (hourly job owns the site).
Decision: negative ladder rung forty-four ([-1]x44 at -1/44) CANNOT fit the octet in factored-rungs-nine/ (17 + 8 = 25 > 24 — create factored-rungs-ten per the a36/a40/a46/a51/a54/a57/a60/a62/a64 pattern FIRST, before wiring). Prelim outlook: projects ~217 decl / ~222 theorem-theorem — still far under the operative 444 limit, so the octet shape (no trim/split) remains viable; re-evaluate the head-trim only if a future projection approaches the OPERATIVE limit, not the 200 packing convention. Fold this round's calibrations (bare-`[-1]` doc-line extension, bp ordinal-conversion ordering) into the transform from the start. The negative-family induction question remains open.
## 2026-09-14 — Negative ladder rung forty-four: ([-1]x44) inv carries -1/44 via existence-head octet in factored-rungs-ten

Proved ([-1]x44) inv carries -1/44: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a65 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): forty-fourth base crossing <131,132,129,128,neg> rejoins the prior rung's fresh arcs 129/128 per the add-gluing pattern (fresh arcs 131/132, +3 arc pattern, arc 130 unused), switching to <132,129,128,131,pos>; matrix <col131,col132,col0,col3> = <-42,-41,2,3>, fraction (col132-col131)/(col132-col3) = 1/-44. Same neg_ne_zero pattern as rungs one to forty-three for the negative denominator multiple.
New container (per the sub-chapter rule): factored-rungs-nine/ was at 17 articles and the octet cannot fit (17 + 8 = 25 > 24), so created factored-rungs-ten/ FIRST with its README (a66 octet's 8 nodes in the Nodes list, same style as the a64/a65 entries; prose "Rung forty-four and above" plus the 17-article sequel rationale, matching the nine/eight pattern — old containers keep their "and above" prose uncapped, per the seven precedent). Parent neg-ladder README extended by list addition only (factored-rungs-ten link in both the prose chain and the Nodes list), exactly the rung-42 container-transition shape; factored-rungs-nine untouched. (Task-text note: the brief asked to "update the neg-ladder top README prose chain" for "Rung forty-two and above" — no such prose exists in the top README (same rung-43 finding); the "and above" prose lives in the containers, and nine's already covers its rungs uncapped; followed the precedent.)
Arc derivation (no brief indices this round — derivation tasked, not given): read from disk, verified mathematically BEFORE generating. Rung-43 coloring has col128=-41, col129=-40 (def tail cells) and arcs 127/130/131/132 default 0 (absent from the def — confirmed by probe). Fresh arcs 131/132, arc 130 unused: base <131,132,129,128,neg> switching to <132,129,128,131,pos> = (b,c,d,a) per the mirror-switch pattern; rule (col132=col128 ∧ col129+col131=2*col132) forces col132=-41, col131=-42, reading -41=-41 ∧ -40+-42=-82=2*-41; matrix <-42,-41,2,3> over retained col0=2, col3=3; fraction (-41 - -42)/(-41 - 3) = 1/-44 via divInt normalized to -1/44.
Size decision (measured against the OPERATIVE limit, not the packing convention): the transformed prelim's true span is 217 decl-start to next-decl-line (222 theorem-line to next-theorem-line, exactly the journaled projection). NO head-trim or split applied — the audit's operative limit is max(200, 4*median) = 444 on this tree, so 222 passes with 2x margin; a trim node would have broken the octet/count/filename spec. Octet retained: crossings prelim + fraction-value + thin universal + witness def + witness-rules + witness-value + existence-head + thin existence with one-line exacts. True spans decl-convention measured from disk, rung43 → rung44 [prelim 212→217, value 124→126, universal 192→196, def 17→17, rules 136→139, witval 15→15, head 129→132, existence 102→104] (deltas +5/+2/+4/0/+3/0/+3/+2, the exact rung-40→41 and rung-42→43 delta shape; all under the operative limit). Wired as a66/a66a/a66b/a66c/a66d/a66e/a66f/a66g in factored-rungs-ten/ (container now 9 articles: README + octet). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence/existence-head 16384; def and witness-value none); prelim prose records the measured 217/audit-222-vs-444 rationale.
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence/existence-head 16384 from the start; witness def and witness-value carry no override. The rung-39 indent calibration held — top-level certs at 4sp (have at 2sp), nested c131132/c1323 certs at 6sp (have at 4sp), asserted by indent gates; no misparse.
Tactic policy (proof-speed decision, retained): `linear_combination` with solved certificates for the four ladder tails (hnum/hden44/c131132/c1323, generators re-validated against rungs 41/42/43 on disk before trusting for 44); `native_decide` for the 44-crossing prelim membership block; `decide` for the existence-head hcs equation and all small sites. Witness extends the rung-43 coloring by the forced pair (col131,col132)=(-42,-41) (arc 130 unused, defaults 0). Hand-verified PD code: switched <132,129,128,131,pos> is (b,c,d,a) of base <131,132,129,128,neg> per the mirror-switch pattern; witness rules col132=col128 (-41) and col129+col131=2*col132 (-40 + -42 = -82); fraction 1 / -44 via divInt (-41 - -42)/(-41 - 3) = 1/-44 normalized to -1/44.
Transform-gate confirmations (all measured from disk on the added lines): `_negOne` 264 = 6 name lines x44 (chain = `invert_add` + 44x `_negOne`, no doubled underscore); `[-1]` 132 = 3 doc lines x44; `-1/44` 9 sites; `-1 / 44` 2 sites; no `1/-44` in Lean; no `forty-fourth`-in-Lean (CamelCase `FortyFourth` identifiers per the FortyThird convention; `forty-four` numeral form in doc comments); blueprint `forty-fourth` prose (prelim: "the forty-fourth base crossing") + 8 forty-fourth filename link targets (+ 8 container-README Nodes lines), zero `fortyth`/`fourty`; wrapper-aware exact counting (first line is the `exact ⟨hc …` wrapper; 44 `hc ⟨⟩` applications, 44-crossing rcases + 44 one-line exacts verified complete); hcs-list 44 + prelim 44 crossings scale per the +3 arc pattern; LF-only throughout (blueprint nodes LF, zero literal-backslash-n, all 8 frontmatter lean names resolve — check resolved 322, +8). Script defects caught pre-write by gates (no bad state committed): (1) chain-start regex missed the `(hc :` / `have hcs :` prefixed starts — switched to the paren-run match (`\({3,}` before `crossingTangle`), gated 9 sites; the inserted continuation duplicates the following line (diff shape), which matters because `have hcs` starts at 2sp while its continuations sit at 6sp; (2) the bare-pair head replacement `e42b + e43a` also occurs mid-cert in c128129 — anchored to the `linear_combination` head (2 sites: hden44 + c1323 only); (3) `forty-three`→`forty-four` must run as one global (the fraction doc splits it across a line break, so a same-line-only rename misses it) with the witness-def `forty-two`→`forty-three` prior-rung fix ordered AFTER it; (4) blueprint `[-1]` runs are abbreviated at 38 across rungs (a64/a65 precedent) — gated unchanged, not extended. The file's sole DiagonalSumGeneral warning is still the pre-existing line-399 `hok₁` (98 warnings total, identical to baseline).
Cost: `lake build` green FIRST try (1027 jobs), zero sorry/admit/axiom (`grep -w` clean). Check 353 articles / 321 fully proved + 1 proved (322 settled; +8/+8 — the def node resolves by elaboration), 322 declarations resolved, audit clean True with findings [], render 388 pages / 323 nodes / 721 code links (+10/+8/+8 — pages include the new container index plus per-node dependency views). No `mkdocs gh-deploy` (hourly job owns the site).
Decision: negative ladder rung forty-five ([-1]x45 at -1/45) FITS the octet in factored-rungs-ten/ (9 + 8 = 17 <= 24, no new container — journal the plan, do NOT execute it). Prelim outlook: projects ~222 decl / ~227 theorem-theorem — still far under the operative 444 limit, so the octet shape (no trim/split) remains viable; re-evaluate the head-trim only if a future projection approaches the OPERATIVE limit, not the 200 packing convention. Fold this round's calibrations (paren-run chain matching, head-anchored cert replacement, global-then-prior-rung bp ordering) into the transform from the start. The negative-family induction question remains open.
## 2026-09-14 — Negative ladder rung forty-five: ([-1]x45) inv carries -1/45 via existence-head octet in factored-rungs-ten

Proved ([-1]x45) inv carries -1/45: universal claimed over TWO factored preliminaries (crossings rules plus the fraction-value lemma holding the universal's linarith tail) plus direct witness, mirroring a66 with fresh mirror-switch PD derivations re-derived from the switch definition (never negated positive codes): forty-fifth base crossing <134,135,132,131,neg> rejoins the prior rung's fresh arcs 132/131 per the add-gluing pattern (fresh arcs 134/135, +3 arc pattern, arc 133 unused), switching to <135,132,131,134,pos>; matrix <col134,col135,col0,col3> = <-43,-42,2,3>, fraction (col135-col134)/(col135-col3) = 1/-45. Same neg_ne_zero pattern as rungs one to forty-four for the negative denominator multiple.
No new container (per the sub-chapter rule): factored-rungs-ten/ was at 9 articles (README + octet) and the octet fits (9 + 8 = 17 <= 24); README Nodes list extended with the a67 octet, prose "Rung forty-four and above" already covers rung forty-five (old containers keep their "and above" prose uncapped, per the seven precedent), parent neg-ladder README untouched (container list unchanged), factored-rungs-seven/eight/nine untouched. (Task-text note: the brief asked to "update the neg-ladder top README prose chain" for "Rung forty-three and above" — no such prose exists in the top README (same rung-43/rung-44 finding; verified by grep — the only "and above" prose is in the containers); followed the precedent.)
Arc derivation (no brief indices this round — derivation tasked, not given): read from disk, verified mathematically BEFORE generating. Rung-44 coloring has col131=-42, col132=-41 (def tail cells) and arcs 130/133/134/135 default 0 (absent from the def — confirmed by probe); retained col0=2, col3=3. Fresh arcs 134/135, arc 133 unused: base <134,135,132,131,neg> switching to <135,132,131,134,pos> = (b,c,d,a) per the mirror-switch pattern; rule (col135=col131 ∧ col132+col134=2*col135) forces col135=-42, col134=-43, reading -42=-42 ∧ -41+-43=-86=2*-42; matrix <-43,-42,2,3> over retained col0=2, col3=3; fraction (-42 - -43)/(-42 - 3) = 1/-45 via divInt normalized to -1/45.
Size decision (measured against the OPERATIVE limit, not the packing convention): the transformed prelim's true span is 222 decl-start to next-decl-line (227 theorem-line to next-theorem-line, exactly the journaled projection). NO head-trim or split applied — the audit's operative limit is max(200, 4*median) = 444 on this tree, so 227 passes with ~2x margin; a trim node would have broken the octet/count/filename spec. Octet retained: crossings prelim + fraction-value + thin universal + witness def + witness-rules + witness-value + existence-head + thin existence with one-line exacts. True spans journal-convention measured from disk, rung44 → rung45 [prelim 217→222, value 126→128, universal 196→200, def 17→17, rules 139→142, witval 15→15, head 132→135, existence 104→106] (deltas +5/+2/+4/0/+3/0/+3/+2, the exact rung-40→41 / rung-42→43 / rung-43→44 delta shape; all under the operative limit). Wired as a67/a67a/a67b/a67c/a67d/a67e/a67f/a67g in factored-rungs-ten/ (container now 17 articles). Blueprint depth flags honest (crossings prelim 4096, value 8192, universal/existence/existence-head 16384; def and witness-value none); prelim prose records the measured 222/audit-227-vs-444 rationale.
Depth setup (per standing orders): prelim/rules 4096, value 8192, universal/existence/existence-head 16384 from the start; witness def and witness-value carry no override. The rung-39 indent calibration held — top-level certs at 4sp (have at 2sp), nested c134135/c1353 certs at 6sp (have at 4sp), asserted by indent gates; no misparse.
Tactic policy (proof-speed decision, retained): `linear_combination` with solved certificates for the four ladder tails (hnum/hden45/c134135/c1353, generators re-validated by reproducing the rung-44 certs byte-identical from disk before trusting for 45); `native_decide` for the 45-crossing prelim membership block; `decide` for the existence-head hcs equation and all small sites. Witness extends the rung-44 coloring by the forced pair (col134,col135)=(-43,-42) (arc 133 unused, defaults 0). Hand-verified PD code: switched <135,132,131,134,pos> is (b,c,d,a) of base <134,135,132,131,neg> per the mirror-switch pattern; witness rules col135=col131 (-42) and col132+col134=2*col135 (-41 + -43 = -86); fraction 1 / -45 via divInt (-42 - -43)/(-42 - 3) = 1/-45 normalized to -1/45.
Transform-gate confirmations (all measured from disk on the added lines): `_negOne` 270 = 6 name lines x45 (chain = `invert_add` + 45x `_negOne`, no doubled underscore); `[-1]` 135 = 3 doc lines x45 (universal bare-run line extended gen_rung42-style, def/existence `[-1]inv` → `[-1]+[-1]inv`); `-1/45` 9 sites; `-1 / 45` 2 sites; no `1/-45` in Lean; no `forty-fifth`-in-Lean (CamelCase `FortyFifth` identifiers per the FortyFourth convention; `forty-five` numeral form in doc comments); blueprint `forty-fifth` prose + 8 forty-fifth filename link targets (+ 8 container-README Nodes lines), zero `fourty`; wrapper-aware exact counting (first line is the `exact ⟨hc …` wrapper; 45 `hc ⟨⟩` applications, 45-crossing rcases + 45 one-line exacts verified complete); hcs-list 45 + prelim 45 crossings scale per the +3 arc pattern; LF-only throughout (blueprint nodes LF, zero literal-backslash-n, all 8 frontmatter lean names resolve — check resolved 330, +8). Three script defects, all caught pre-write by gates (no bad state committed): (1) the rcases/exact-w45 list rebuild used `[l + (X if cond else l)]` — the else branch doubled EVERY non-matching line inline (339→678-char lines); fixed to bare-`l` else, gated by line-length/total-_negOne checks; (2) the prior-rung blueprint fix targeted ordinal `forty-third`, but the witness doc uses the NUMERAL form (`forty-three-unit`, per the rung-44 journal) — switched to numeral ordering (ordinal, then numeral, then prior-rung numeral last), gated 1 fix in a67c only; (3) the c-cert rename missed the use site `exact hmRw ⟨c131132, c1323⟩` (have-lines renamed, use-site not) — added the use-site replacement, gated. The file's sole DiagonalSumGeneral warning is still the pre-existing line-399 `hok₁` (98 warnings total, identical to baseline).
Cost: `lake build` green FIRST try (1027 jobs; DiagonalSumGeneral module 197s), zero sorry/admit/axiom (`grep -w` clean). Check 361 articles / 329 fully proved + 1 proved (330 settled; +8/+8 — the def node resolves by elaboration), 330 declarations resolved, audit clean True with findings [], render 396 pages / 331 nodes / 729 code links (+8/+8/+8). No `mkdocs gh-deploy` (hourly job owns the site).
Decision: negative ladder rung forty-six ([-1]x46 at -1/46) CANNOT fit the octet in factored-rungs-ten/ (17 + 8 = 25 > 24 — create factored-rungs-eleven per the a36/a40/a46/a51/a54/a57/a60/a62/a64/a66 pattern FIRST, before wiring; journaled here, NOT executed). Prelim outlook: projects ~227 decl / ~232 theorem-theorem — still far under the operative 444 limit, so the octet shape (no trim/split) remains viable; re-evaluate the head-trim only if a future projection approaches the OPERATIVE limit, not the 200 packing convention. Fold this round's calibrations (bare-`l` else in list rebuilds, numeral-form prior-rung ordering, hmRw use-site rename alongside the have-lines) into the transform from the start. The negative-family induction question remains open.
